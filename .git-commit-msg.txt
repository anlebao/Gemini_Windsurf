[VALCN-V2 P0+P1] Domain fields + LoyaltyIssuanceRecord + FeatureFlag infra + migration

Phase 0 (ANALYZE): Verified BOM v2.0 claims against codebase. Found PlatformFeeRate
is global not per-tenant (fixed: added per-tenant field). Confirmed LoyaltyIssuanceRecord
needed (History JSON lacks structured OrderId). Confirmed AccountingEntry factory chain
safe (147 callers, optional param backward compatible).

Phase 1 (IMPLEMENT):
- 12 additive fields: 6 budget caps on LoyaltyTenantConfig, PlatformFeeRate on
  ShopFeatureSettingsEntity (per-tenant, default 5%), CorrelationId on AccountingEntry
  + OutboxMessage, PlatformFeeAmount on Order
- 1 new entity: LoyaltyIssuanceRecord (per-order loyalty tracking for Phase 4 reversal)
- AccountingEntry factory chain: 4 factory methods + constructor + CreateReversal
  preserve CorrelationId (fix M3)
- OutboxRepository: propagate CorrelationId from OutboxEvent to OutboxMessage
- SimpleAccountingEventHandler: set CorrelationId = Order.Id on accounting entries
- OrderWorkflowService: set CorrelationId on OutboxEvent + create LoyaltyIssuanceRecord
  on AddPoints (Alliance + Silo modes)
- FeatureFlag infrastructure: IFeatureFlagService + FeatureFlagService (default OFF,
  30s cache, SystemSetting-backed) + FeatureFlagsController (SystemAdmin JWT) +
  FeatureFlagApiClient (ShopERP HTTP client) + ValcnFeatures.razor admin UI +
  NavMenu link
- 1 EF migration: AddValcnV2PlatformLightFields (9 columns + 1 new table + 1 index)
- 3 feature flag keys: ValcnV2_PlatformFee, ValcnV2_LoyaltyBudget, ValcnV2_RefundReversal
  (all default OFF — existing behavior preserved until admin enables)

Build: 0 errors. All features OFF by default — no production impact until toggled.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
