[VALCN-V2 P3] Loyalty budget enforcement + reset jobs (feature-flagged, default OFF)

Phase 3 — check budget caps before AddPoints + 2 reset jobs (daily/monthly).
When ValcnV2_LoyaltyBudget flag is OFF (default), existing behavior preserved
(AddPoints directly, no budget check). When ON, caps applied:
  - PerOrderRateCap, MonthlyPointsBudget, DailyPointsBudget, PerCustomerDailyLimit

Changes:
- ILoyaltyBudgetService: interface (CheckAndAdjustPointsAsync, RecordIssuanceAsync,
  DecrementIssuanceAsync, ResetAllDailyCountersAsync, ResetAllMonthlyCountersAsync)
- LoyaltyBudgetService (CoreHub): direct PG impl — atomic ExecuteUpdateAsync for
  counter increments (fix I1 race condition), per-customer daily check via
  LoyaltyIssuanceRecord query, INV-009 deferred (no PointValue field in
  LoyaltyGlobalConfig — v3.0)
- LoyaltyBudgetController (Gateway internal API): POST /api/internal/loyalty-budget/
  {check-adjust,record,decrement} — [InternalApiKey] auth (service-to-service)
- LoyaltyBudgetServiceHttpProxy (ShopERP): HTTP proxy to Gateway internal API
  (ShopERP SQLite ignores LoyaltyTenantConfig — PG-only). Graceful degradation:
  Gateway unreachable → no cap applied (safe fallback)
- OrderWorkflowService: inject ILoyaltyBudgetService + IFeatureFlagService (both
  optional, null in test contexts). Budget check inserted after pointsToAward
  calculation, before AddPoints. RecordIssuanceAsync called after AddPoints
  succeeds (both Alliance + Silo branches). Budget exhausted (adjusted=0) →
  skip reward, order still completes
- LoyaltyBudgetDailyResetJob: daily 00:00 UTC, resets PointsIssuedToday=0 for all
  tenants. IServiceProvider + CreateScope pattern (matches CoolingPeriodJob).
  Toggleable via BackgroundServiceToggleService
- LoyaltyBudgetMonthlyResetJob: 1st of month 00:00 UTC, resets
  PointsIssuedThisMonth=0 for all tenants. Same pattern
- BackgroundServiceToggleService: 2 new KnownServices entries (Gateway VPS)
- DI: Gateway registers LoyaltyBudgetService (Scoped) + 2 jobs (HostedService).
  ShopERP registers LoyaltyBudgetServiceHttpProxy (Scoped, HTTP proxy)

Build: 0 errors (1371 warnings, pre-existing).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
