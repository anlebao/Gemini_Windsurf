[FIX] KhachLink: Google login redirect + Android PWA install

1. Google login "redirect sai, bat dang nhap lai":
   - Gateway forward controllers (Customers/Loyalty/CustomerIdentity/Dashboard)
     returned text/plain instead of application/json when forwarding responses.
   - KhachLink Profile page checks Content-Type for "json" — non-JSON response
     was treated as invalid token, removing it and forcing re-login.
   - Fix: use ContentResult with upstream Content-Type preserved.

2. Android PWA install ("app chua duoc tren android"):
   - service-worker.js cached non-existent files (/css/app.css, /js/app.js,
     /images/logo.png) → cache.addAll() rejected → SW install failed →
     Chrome Android never showed install prompt.
   - Fix: updated staticUrlsToCache to actual files, bumped cache version.
   - manifest.json: added purpose "any maskable" to 192x192 and 512x512
     icons (required by Android Chrome for installability).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
