fix(#134): Enforce disable at runtime — clear stale cache + SW v18 + shorter TTL

Problem: timlathay.com (disabled instance) still renders FullCommerce layout
with all icons (cart, rewards, login). Root cause: stale localStorage cache
from old format (no IsActive field → deserialized as true) + SW cache not
bumped → browser served old WASM → disabled check never ran.

Fix (3 layers):
1. SW cache version bumped v17 → v18-instance-disable (forces SW update +
   clears old caches). SW activate event now postMessage
   CLEAR_INSTANCE_CONFIG_CACHE to all clients.
2. pwa.js message handler clears stale khachlink_instance_config localStorage
   keys when receiving CLEAR_INSTANCE_CONFIG_CACHE from SW.
3. KhachLinkInstanceHttpService cache key changed khachlink_instance_config
   → khachlink_instance_config_v2 (invalidates old format cache). TTL reduced
   from 5 min → 1 min so deactivation takes effect faster.
4. Gateway CORS SetPreflightMaxAge = 60s (was browser default ~5 min) so
   preflight cache doesn't delay enforcement of disabled instances.

Build: 0 errors. Guard-check: ALL PASSED.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
