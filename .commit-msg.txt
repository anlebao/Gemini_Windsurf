fix(#134): CORS must allow disabled domains so WASM can fetch config + show disabled page

Root cause of "timlathay.com vẫn vào được, show đủ icons":
GetActiveCustomDomainsAsync filtered IsActive=true → disabled instance's domain
removed from CORS snapshot → CORS blocked KhachLink WASM from fetching by-domain
config → WASM caught error → fell back to FullCommerce defaults (all icons shown).

Fix: GetActiveCustomDomainsAsync now returns ALL custom domains (including disabled).
CORS allows the fetch → WASM gets config with IsActive=false → KhachLinkLayout
shows "Instance tạm ngưng" disabled page (from previous commit).

Build: 0 errors. Guard-check: ALL PASSED.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
