fix(#134): ByDomainResponse.Profile is string not int — JSON deserialize was failing

ROOT CAUSE: API returns "profile":"Directory" (string) but ByDomainResponse.Profile
was typed as int → System.Text.Json throws "InvalidCast, String, number" →
GetByCurrentDomainAsync catches exception → returns null → MainLayout falls back
to FullCommerce defaults → disabled instance still renders with all icons.

FIX: Changed ByDomainResponse.Profile from int to string. Parse via
Enum.TryParse<KhachLinkProfile> with FullCommerce fallback.

Build: 0 errors.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
