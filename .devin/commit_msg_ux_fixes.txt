fix(khachlink): 3 UX fixes — faster Home load, silent SW update, hide label

Issue 1: Home page load 6-8s → ~1-2s (parallel HTTP + video loading)
- Home.razor OnInitializedAsync: moved localStorage calls to Task.WhenAll
  (cart + recently viewed run in parallel, no HTTP)
- Home.razor OnAfterRenderAsync: 4 HTTP calls now run concurrently via Task.WhenAll
  (products + catalog + campaigns/store + VAT toggle). ShopConfig loads after
  products (dependent). Was 5 sequential HTTP → now 2 phases (4 parallel + 1 dependent)
- index.html: replaced spinner loading screen with cocktail video
  (https://shutterstock.com/.../3915506465/preview/...webm).
  Video plays during WASM boot + HTTP load. Falls back to spinner if video fails.

Issue 2: KhachLink auto-reload ~60s on Home page → silent SW update
- pwa.js: removed window.location.reload() on controllerchange event.
  Was causing disruptive page refresh when Chrome checked for SW updates
  (default every ~60s when page visible). Replaced with subtle toast
  "App đã cập nhật — nhấn để tải lại" (auto-dismiss 8s, click to reload).
- service-worker.js: bumped cache version v12-sri-fix → v13-silent-update
  to trigger SW update for existing clients.

Issue 3: Hide "Đã kết nối" label, keep wifi icon
- PWAInstallPrompt.razor: removed "Đã kết nối" text when online.
  Online state now shows icon-only (green wifi circle, 32px, opacity 0.6).
  Offline state still shows "Ngoại tuyến" text with red pulse animation.
- CSS: .offline-indicator.online → border-radius:50%, width/height:32px,
  opacity:0.6 (subtle, non-intrusive).

Verified: Build 0 errors, guard-check ALL PASSED

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
