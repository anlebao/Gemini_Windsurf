feat(crm): AF-P3 cosmetic — extract PromoPushComposer + RecipientConfiguration + CSV export

P3-T1: Extract PromoPushComposer.razor from CustomerList.razor inline modal.
  New component at 5_WebApps/ShopERP/Components/Pages/Admin/PromoPushComposer.razor
  owns form fields (title/message/url) + reset-on-show. Parent passes Show,
  RecipientCount, IsSubmitting, ErrorMessage; receives PromoPushPayload via OnSubmit.
  CustomerList.razor: removed 56 lines of inline modal markup + 3 form-state fields
  + ResetPromoForm helper; SubmitPromoAsync now accepts PromoPushPayload.

P3-T2: Extract PromoCampaignRecipientConfiguration.cs to its own file
  (3_CoreHub/Infrastructure/Configurations/PromoCampaignRecipientConfiguration.cs).
  Same namespace, same logic. PromoCampaignConfiguration.cs trimmed to single class.

P3-T3: Add POST /api/customers/export endpoint (CustomerController.ExportCsv).
  Accepts SegmentRequest body (same as /segment), returns CSV with columns
  Name,Phone,Tier,Points,TotalSpent,LastOrder,Birthday,IdentityLevel,HasPush.
  Content-Type: text/csv, Content-Disposition: attachment; filename=customers.csv.
  Minimal CSV escaping (quote-wrap on comma/quote/newline, doubles inner quotes).

Build: 0 errors, 1117 warnings (pre-existing CA). No domain layer changes.
Closes audit deviations D5, D7, D13.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
