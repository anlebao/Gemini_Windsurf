feat(crm): AF-P2 UX completions — per-row/bulk promo send + progress bar + detail expand + push column

P2-T1: Per-row "Gửi" button in CustomerList — opens promo modal with single customer ID
P2-T2: Checkbox bulk select (cross-page) + "Gửi cho N đã chọn" button — explicit ID list
P2-T3: Progress bar in PromoCampaignList for Processing rows (SentCount/TotalRecipients %)
       + auto-refresh every 5s via PeriodicTimer while any campaign is Processing/Pending
P2-T4: "Chi tiết" expand/collapse per campaign row — loads recipients via service
       (paginated 20/page with "Tải thêm") + customer name enrichment
P2-T5: "Push" column in CustomerList (check/cross icon) — CustomerDto.HasPushSubscription
       via batch-loaded IPushSubscriptionRepository.GetAllActiveAsync per request

Backend:
- IPromoCampaignService: new CreateCampaignAsync(title, msg, url, IReadOnlyList<Guid>) overload
- PromoCampaignService: implements explicit-ID overload (resolves via ICustomerRepository,
  skips unknown/inactive, snapshots ID list for audit)
- PromoCampaignController.Create: accepts SelectedCustomerIds (non-empty = explicit flow,
  else falls back to segment criteria)
- CustomerController: injects IPushSubscriptionRepository, batch-loads push CustomerIds,
  MapCustomerDto gains hasPushSubscription param (default false = backward compatible)

Build: 0 errors, 1113 warnings (pre-existing CA). Core.Tests 1023/1037 PASS (Release).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
