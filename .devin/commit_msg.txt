fix(khachlink): hide Alliance Wallet UI when LoyaltyMode=Silo

When SystemAdmin sets LoyaltyMode=Silo, KhachLink now hides all
Alliance Wallet UI to prevent customer confusion:

- NavMenu.razor: hide Alliance Wallet tab (desktop sidebar + mobile bottom-nav)
- LoyaltyCard.razor: hide cross-tenant alliance wallet link card
- AllianceWallet.razor: direct-nav guard shows feature-off message

New public endpoint: GET /api/loyalty/mode (anonymous) returns global
LoyaltyMode (Silo | Alliance). KhachLink queries this on page load
via LoyaltyModeHttpService (cached 5 min, defaults to Silo on error).

Build: 0 errors. LoyaltyWalletControllerTests updated for new ctor param.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
