feat: #100 cải tiến layout KhachLink — 4 phần

#100 — Cải tiến layout KhachLink (4 phần)

1. Mobile UX — sticky action bar trên trang giỏ hàng:
   - Cart.razor: thêm .cart-mobile-actionbar (d-md-none, sticky top:56px)
     hiển thị tổng tiền + nút "Thanh toán" fixed ở đầu viewport.
     Khách hàng mobile không cần lướt xuống cuối để bấm thanh toán.

2. Home KhachLink — SystemAdmin toggle on/off 4 sections:
   - ShopFeatureSettingsDto: thêm Home_CampaignSection_Enabled,
     Home_StoreSection_Enabled, Home_FeaturedSection_Enabled,
     Home_SocialHub_Enabled (default ON).
   - ShopFeatureSettingsEntity + UpdateToggles: persist 4 new toggles.
   - Migration: AddHomeSectionToggles (4 nullable boolean columns).
   - ShopFeatures.razor: thêm "Hiển thị trang chủ KhachLink" card
     với 4 toggles (Campaign, Store, Featured, SocialHub).
   - Home.razor: LoadHomeSectionTogglesAsync() load toggles in parallel.
     @if (_homeCampaignEnabled) wrap Campaign section.
     @if (_homeStoreEnabled) wrap Store section.
     @if (_homeFeaturedEnabled) wrap Featured section.
     @if (_homeSocialHubEnabled) wrap SocialHub component.

3. SystemAdmin đổi link FB/TikTok trên home KhachLink:
   - Đã có sẵn: TenantManagement.razor có SocialLinksFb/SocialLinksTiktok
     fields. SocialHub component đọc từ ShopConfig.SocialLinksFb/
     SocialLinksTiktok. Flow hoàn chỉnh — không cần thay đổi thêm.

4. ShopERP shop config save — hiển thị thông báo:
   - ShopFeatures.razor SaveSettings: thêm _isSaving loading state.
     VanAButton Loading="@_isSaving" Disabled="@_isSaving"
     Text="@(_isSaving ? "Đang lưu..." : "Lưu cấu hình")".
   - successMessage: "✅ Đã lưu cấu hình thành công." + auto-dismiss 5s.
   - errorMessage: "❌ Không thể lưu cấu hình: {ex.Message}".
   - VanAAlert success/error đã có sẵn trong markup (lines 36-43).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
