fix(cd-multivps): map all 14 secrets to env vars with matching names

Pre-Deployment Validation failed with "Missing POSTGRES_PASSWORD" and
"Missing SHOP_INSTANCE_ID" even though secrets exist. Root cause: env
mapping used shortened names (GATEWAY_HOST, SHOP_INSTANCE) while the
check loop iterated over full secret names (VPS_GATEWAY_HOST,
SHOP_INSTANCE_ID, POSTGRES_PASSWORD). POSTGRES_PASSWORD + 4 others
had no env mapping at all. Fix: map all 14 secrets 1:1 to env vars
with identical names.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
