fix(#99-3): loyalty dashboard — fix OrderStatus LINQ translation (Status value object)

Root cause: Queries used `o.Status.Value != "completed"` which fails
LINQ translation (OrderStatusId is a record value object, .Value
access not translatable by EF Core — same as Pattern #8 for TenantId).

Fix: Use `o.Status != OrderStatusId.Completed` pattern (matches
DashboardController convention). "delivered" status has no static
prop in OrderStatusId — use `new OrderStatusId("delivered")`.

Affected queries: Orders (metric 3 + metric 4).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
