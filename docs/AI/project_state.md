# Project State

> **Mục đích:** Single Source of Truth cho AI về trạng thái dự án. BẮT BUỘC đọc đầu mỗi phiên.
> **Archived:** 2026-07-24 + 2026-08-03 + 2026-08-09 + 2026-08-23 + 2026-09-06 + 2026-09-15 + 2026-09-17 — All completed objectives + full history/maintenance log moved to `docs/AI/project_state_archive.md`

---

## 0. Maintenance Rules

1. One-and-only-one: Mỗi section chỉ tồn tại 1 lần.
2. No contradiction: Một hạng mục chỉ có 1 trạng thái.
3. Ground Truth first: Verify path/branch với codebase trước khi ghi.
4. Now over History: Section 2-4 chỉ mô tả việc ĐANG làm và KẾ TIẾP. Việc xong gom vào archive.
5. Actionable Next Actions: Xóa action đã quá hạn/sai bối cảnh.
6. Stamp every edit: Cập nhật Section 10 mỗi lần sửa.

---

## 1. Project Overview

**Dự án:** Vạn An Accounting System MVP — giải pháp kế toán HKD theo TT 152/2025/TT-BTC.
**Stack:** .NET 8 — EF Core — SQLite — Blazor Server (ShopERP) — Blazor WebAssembly (KhachLink PWA) — Blazor SSR (Directory) — SignalR — YARP Gateway — xUnit — Playwright.
**Kiến trúc:** Clean Architecture + DDD + Multi-tenancy. Data flow: `KhachLink WASM/SSR (5002) -> Gateway (5001) -> ShopERP (5003) -> SQLite`.
**Modules:** `1_Shared` (Domain + Services contracts) — `2_Gateway` (YARP) — `3_CoreHub` (Services, in-process) — `5_WebApps/ShopERP` (Blazor Server) — `5_WebApps/KhachLink` (Blazor WASM, served by nginx) — `5_WebApps/Directory` (Blazor SSR, Directory-profile tenants) — `UI.Platform` (Shared components) — `6_Tests`.
**Hard stops:** Domain PURE — `AccountingEntry` immutable — Gateway = Order Creator + Routed Async Delivery (Option C) — KhachLink HTTP-only — ShopERP SQLite (Business) + PostgreSQL (Accounting) — ALWAYS dùng UI Platform components.

**VPS Access (GCP — for RV + manual deploy):**
- GCP project: `vanan-prod` (gcloud SDK at `C:\Users\lebao\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin\gcloud.cmd`)
- SSH command pattern: `gcloud compute ssh <INSTANCE_NAME> --zone <ZONE> --project vanan-prod`
- Instances (4): `vanan-gateway` (asia-southeast1-a, 136.85.94.119) · `vanan-shop-a` (asia-southeast1-b, 34.177.89.248) · `vanan-khachlink` (asia-southeast1-c, 136.85.111.51) · `vanan-khachlink-20260815-timlathay-com` (asia-southeast1-c, 136.85.78.51)
- CD: `cd-multivps.yml` (push to `main`) deploys to all 3 VPS + smoke tests. Legacy `cd.yml` (push to `oracle-prod`) — SSH broken since 2026-08-06, use multi-VPS CD only.

---

## 2. Current Objective

**LOYALTY POINTS INTEGRITY — ✅ BATCH 1-5 DEPLOYED + RV PRODUCTION PASS (2026-09-21) — ✅ BUG-1 + BUG-1b FIXED + DEPLOYED + RV PRODUCTION PASS (`fac4e979` + `693f2763`, CD Multi-VPS SUCCESS ×2) — hết bug đã biết (còn 1 finding nhỏ: ordering sync events, xem Section 3).**

> Batch 2 commits: `962fa197` (ledger core) + `6b9a667e` (backfill FK fix) + `5f7f747d` (subscriber race retry) — CD Multi-VPS SUCCESS ×3. Rollback nhanh: tắt flag `LoyaltyLedgerV2` (default ON).

Detail coding plan: `docs/plans/loyalty-integrity-detail-coding-plan.md` (5 phases, 4 batches).
Origin: user bug — "sau khi đơn thành công, điểm thưởng được tính nhưng không chính xác, cũng không cộng vào cho khách hàng". Review tìm 3 root causes + 3 yêu cầu sâu (tenant attribution, budget validity, mode spend rules: Silo chỉ tiêu tại tenant đã tặng / Alliance tiêu mọi tenant).

**Batch 1 DONE + DEPLOYED + RV PASS (`ebbc1f5a` + `6023ad0`, CD Multi-VPS SUCCESS ×2):** fix RC1 (Silo award trên Gateway ghi PG, khách đọc SQLite → không bao giờ thấy điểm).
- `LoyaltyBalanceSyncPublisher` (mới): subject `vanan.cloud.loyalty.changed.{deviceId}` + payload mở rộng (customerId/tenantId/type/points/reason/updatedAt) + Outbox `LoyaltyChanged` (RoutingKey) — Silo (LoyaltyRewardsService) + Alliance (AllianceWalletService) dùng chung.
- `LoyaltySyncSubscriber`: match (tenantId,customerId) → device fallback → **tự tạo customer stub + LoyaltyRewards row** khi khách chưa có local (guest Gateway-completed); **max-merge balance** (bảo toàn điểm POS, không ghi đè ngược); history idempotent.
- Backfill `POST /api/admin/sync/loyalty-backfill-pg` (SQLite→PG additive, dedup history, chạy lại an toàn) — chạy TRƯỚC Batch 2 cutover.
- **RV production (đơn thật `01a0becd`, tenant "Vạn An Test" loyalty ON, complete qua Gateway api2):** 🎁 Awarded 30 points (PG) → guard "Skipped duplicate" (delivered+completed) → shop-a prod: `created customer stub eb5b6a96` → `created LoyaltyRewards row` → `appended history (EARN,30)` → **`synced balance → 30`** — trước fix khách thấy 0.
- Core.Tests **1680 PASS** (+9) · build 0 errors · guard ALL PASSED.

**Batch 3 DONE + DEPLOYED + RV PASS (`2c12852b`, CD Multi-VPS SUCCESS):** budget caps (đăng ký + enforce). **T3.1** Gateway `LoyaltyConfigController` PUT tenant config nhận 4 budget cap (monthly/daily/perCustomerDaily/rateCap 0..1, null = unlimited) + validation âm → 400 + `SetBudgetCaps`; DTO trả kèm counters (`PointsIssuedThisMonth/Today`); `POST /tenant/{id}/reset-counters` (daily|monthly). **T3.2** ShopERP `LoyaltyConfigAdmin.razor` form 4 budget field (trần đơn nhập %) + hiển thị counters + nút Reset hôm nay/tháng (modal xác nhận); client DTOs + `ResetTenantCountersAsync`. **T3.3** `ValcnV2_LoyaltyBudget` **default ON** (flag = emergency off SystemAdmin); OrderWorkflowService legacy path bỏ gate flag — budget check + RecordIssuance luôn chạy (ledger AwardAsync đã unconditional từ Batch 2). **T3.4** verify (không đổi): RefundOrchestration Step 2c qua ledger `RevertOrderAsync` + `DecrementIssuanceAsync`. Tests: `LoyaltyBudgetServiceTests` mới (11) + `LoyaltyConfigControllerTests` +7 → **Core.Tests 1717 PASS · build 0 errors · guard ALL PASSED**. **RV production:** L1 API (PUT persist GET lại OK · monthly=-5 → 400 · rateCap=1.5 → 400 · reset daily/monthly 200 counters 0 · scope invalid 400) · L2 markers Gateway.dll (`MonthlyPointsBudget` + `reset-counters`) · L3/L4 Playwright app2 (sysadmin login → /admin/loyalty-config → chọn "Vạn An Test" → budget form + counters + reset buttons render, 0 page errors) — spec `rv-batch3.spec.ts`. Cleanup: config row xóa (không tồn tại trước RV), không để lại dữ liệu test.

**Batch 4 DONE + DEPLOYED + RV PRODUCTION PASS (`5e9ada23`, CD Multi-VPS SUCCESS — sau cleanup disk vanan-shop-a 100% full):** Phase 4 — 1 công thức duy nhất. **T4.1** `LoyaltyPointsCalculator` (pure static, mới): base = NET revenue = `SubTotal − DiscountAmount` (D1, bỏ VAT + phí ship) · Silo base×rate / Alliance base÷VndPerPoint (Option A, default 1000) · clamp min/max 1 nơi · `OrderWorkflowService` award chuyển sang dùng chung calculator (fix RC2 — trước đây award = TotalAmount×rate gồm VAT+ship, drift với banner/estimate). **T4.2** banner tracking (PublicOrdersController) đọc điểm THỰC TẾ từ ledger `GetAwardedPointsAsync` (D4 — bỏ recompute). **T4.3** `GET /api/loyalty/estimate` (server-side, mode-aware) + KhachLink Checkout.razor gọi endpoint thay vì tự tính client. Tests: `LoyaltyPointsCalculatorTests` (11) + `PublicOrdersControllerTests` (5) → **Core.Tests 1733 PASS (+16) · ShopERP.Tests 99 · guard ALL PASSED · build 0 errors**. **RV production:** L1 — estimate (100k−10k → base 90k → 9.000 pts · discount>subtotal → 0) · banner đơn `01a0becd` completed → `pointsAwarded: 30` = số THẬT (recompute cũ ra 5.500) · pending → null · L2 markers Gateway.dll (`LoyaltyPointsCalculator`) + KhachLink WASM (UTF-16 `loyalty/estimate`+`LoyaltyEstimateDto`) · L3/L4 Playwright `rv-batch4.spec.ts` 2/2 PASS — checkout estimate banner "~4545 điểm" (50.000/1.1×10% — client cũ ra 5.000) + tracking banner "Bạn đã nhận được 30 điểm thưởng!". **Infra:** CD lỗi 2 lần ở "Copy ShopERP deploy files" — root cause `/dev/sda1` vanan-shop-a **100% full** (containerd snapshots 3G + content 1.2G + journal 207M) → user duyệt cleanup: `docker image prune -a -f` (+3.6GB) + `journalctl --vacuum-size=100M` (+107M) → disk 66% → rerun CD SUCCESS. **Batch 5 kế tiếp:** Alliance attribution tiêu điểm (D2 ưu tiên tenant hiện tại, FIFO + settlement). Quyết định user D1-D4 đã chốt (xem plan Section 8).

**Batch 1 đã xong (`49d07bd5` + `7a32a9c7`, CD Multi-VPS SUCCESS, RV production PASS):**
- **#180** — Verify Pending Tenant 400: root cause 2 lớp — (1) Slugify giữ dấu TV → `UpdateSlug` regex reject; (2) **fix v1 NFD+\p{Mn} vẫn fail production vì `VanAn.Gateway.runtimeconfig.json` chạy `System.Globalization.Invariant=true` → Normalize(FormD)/\p{Mn} là NO-OP trên runtime deployed** (probe reflection + /proc/1/maps + restart xác nhận) → fix v2 = **explicit Vietnamese diacritics map** (á→a…đ→d, hoạt động mọi globalization mode) + controller catch ArgumentException → BadRequest rõ ràng + client surface body lỗi. **RV: tenant "Quán Cà Phê Đậm Vị" verify không slug → 200, slug `quan-ca-phe-dam-vi-...`**.
- **#178** — Referral config Free/Charity: Domain `CommissionRate` range 0.02-0.05 → **0-0.5** (đã duyệt), UI default 0.01/1000, tests T18/T18b. **RV: POST rate=0 → 201** (trước 400). Phần 2 (QR list feature) → Batch 2.
- **#177** — Free/Charity order: client-only fix (quyết định đã duyệt) — `ReferralScanResult/DTO` + `ProductType`, `Scan.razor` truyền IsFree qua referral path + QR legacy path (enrich catalog, fallback Free), deserializer + JsonStringEnumConverter. Deployed; UI test L3/L4 pending.

**Batch 2:** ✅ CODE DONE (`6ab09cbc`) + DEPLOYED (CD Multi-VPS SUCCESS) + **RV PASS (2026-09-19)**: #181 hết 2 giỏ (drawer/navCart = 0 tại /cart + /checkout, vẫn hiện trên home) · #178 API 401 + page load OK (luồng salesman login còn lại) · #179 wizard E2E: success hiện **số THẬT từ DB** (32 sản phẩm/15 nguyên liệu) + SQLite verify: tenant test F60B64D0 có 32 products/15 ingredients (TenantId lưu UPPERCASE — SQLite case-sensitive!) · **#176 ĐÃ ĐÓNG ISSUE** (store page 0 crash errors trên deployed bundle).

> Realtime Platform (objective trước — COMPLETE 2026-09-18, P1-P6 + RV) + completed objectives (commission, referral QR, chat+GPS, KhachLink UX, Community Commerce)
> moved to `docs/AI/project_state_archive.md` + maintenance log Section 10 — task card: `docs/AI/tasks/realtime_platform/task_card_01_realtime_chat_gps_reusable.md`.

## 3. Current Status

- **🩹 BUG-1 + BUG-1b FIXED + DEPLOYED + RV PRODUCTION PASS (session 2026-09-21, `fac4e979` + `693f2763`, CD Multi-VPS SUCCESS ×2):**
  - **BUG-1 (Alliance mirror stub không tạo):** `IAllianceWalletService.AddPointsAsync/DeductPointsAsync/RefundAsync` thêm optional `Guid customerId` → `AllianceWalletService` forward vào `LoyaltyBalanceSyncPublisher.PublishAsync` (payload customerId giờ là THẬT, không còn Guid.Empty). Call sites truyền customerId: `LoyaltyPointLedgerService` (award/spend/refund/revert-order) · `OrderWorkflowService` (legacy) · `MissionService` · `RedemptionService` (redeem + cancel) · `InternalLoyaltyController` (DTO + pass-through) · ShopERP `AllianceWalletServiceHttpProxy` (forward body). Raw calls không có customerId giữ Guid.Empty (device fallback, backward compat).
  - **BUG-1b (mirror không bao giờ giảm balance):** `LoyaltySyncSubscriber` bỏ MAX-merge (Batch 1, pre-cutover) → **overwrite với PG authority** (plan T2.2 đã duyệt: "PG giờ là authority → mirror overwrite là đúng"). MAX-merge cũ khiến SPEND không bao giờ giảm mirror → drift tăng vô hạn (RV bắt: SQLite 150 trong khi PG=70 sau redeem).
  - **RV production (E2E D2 đầy đủ qua deployed ledger, tenant flip tạm + cleanup pristine):** mirror stub customer **Id = test customerId (cùng identity PG!)** + LoyaltyRewards row + history EARN 100/EARN 50/**REDEEM −50 (BalanceAfter 70)** + **"synced balance → 70"** — cả 2 bug verify hết trên production. Tests: AW-ATT-7/8 (payload mang customerId; raw call → Guid.Empty) + LPI-B1-7 viết lại (SPEND 400→300 hội tụ) + Moq setups/verifies 7-arg (CS0854) + assert customerId thật (LED-5/6, LA-RD-1, LA-OW-1) → **Core.Tests 1745 PASS · ShopERP.Tests 99 · guard ALL PASSED · build 0 errors**.
  - **Finding nhỏ (ghi nhận, chưa fix):** double-delivery (direct NATS + Outbox) có thể xử lý events lệch thứ tự (1 lần quan sát: REDEEM trước EARN) → overwrite lấy event cuối xử lý → mirror có thể tạm stale giữa các event (display-only — spend thật luôn qua PG ledger; tự hội tụ ở event kế). Fix đúng cần monotonic sequence + migration — defer, chờ Alliance production thật.
- **Loyalty Points Integrity — Batch 5 (session 2026-09-21, `c1db4f6a` + `1b810476`):** ✅ **DEPLOYED + RV PRODUCTION PASS (CD Multi-VPS SUCCESS) — Phase 5 (Alliance attribution khi tiêu điểm, D2 APPROVED).**
  - **RV production 10/10 PASS:** L1 settlement API (empty tenantId → 400 · no-data → zero report) · L2 markers (Gateway.dll `api/platform/loyalty`+`settlement`+`WalletTenantBalance` · Shared.dll `GetTenantBalancesAsync` · CoreHub.dll `ComputeNetEarnByTenantAsync`) · **V7 E2E D2 qua deployed ledger + internal API** (flip tạm "Vạn An Test" `0dfab177` + "Test HKD" `6aaf19e4` → Alliance member): earn A=100 + B=50 → **spend 80 tại B → balance 70, PG đúng 2 REDEEM entries (B→B −50 SourceTenantId=B + A→B −30 SourceTenantId=A)**; Gateway log xác nhận `(sources: 6aaf19e4:50, 0dfab177:30)` — ưu tiên tenant hiện tại + FIFO hoạt động production · settlement A (earned 100/consumed 30/chi-hộ 30/outstanding 70) + B (50/50/0/80/0) đúng · spend 60 tại A → A→A −60 (ưu tiên điểm của A), balance 10 · idempotent replay spend → cached, không double-deduct · **cleanup: PG pristine (0 wallets/0 tx/0 config/0 test customer)** · L3 Playwright regression: `rv-batch4.spec.ts` 2/2 + `rv-batch3.spec.ts` 1/1 PASS (checkout estimate 4545 + tracking banner 30 + loyalty-config admin 0 crash).
  - **🐛 BUG-1 (RV finding, PRE-EXISTING — Batch 1/2 hardening, KHÔNG do Batch 5, chưa fix theo yêu cầu user):** Alliance wallet sync events publish `customerId: Guid.Empty` (wallet device-based) → `LoyaltySyncSubscriber.SyncRowCoreAsync` step 3 tạo mirror stub yêu cầu `customerId != Guid.Empty` → **khách chưa có row local: điểm Alliance award/spend KHÔNG bao giờ sync xuống SQLite** (POS/offline đọc thấy 0). Latent (production mode=Silo, 0 Alliance tenant) nhưng sẽ bite khi tenant chuyển Alliance. Hướng fix (sau): ledger truyền real customerId xuống wallet → publisher, hoặc subscriber tạo device-based stub. Chi tiết: Section 4.
  - Trước đó (code): T5.1 attribution-aware `DeductPointsAsync` (prefer current tenant → FIFO, 1 REDEEM per source w/ SourceTenantId) + `GetTenantBalancesAsync` breakdown · T5.2 settlement report. Core.Tests 1743 PASS (+10) · ShopERP.Tests 99 · guard ALL PASSED · build 0 errors.
- **Loyalty Points Integrity — Batch 4 (session 2026-09-21, `5e9ada23`):** ✅ **DEPLOYED + RV PRODUCTION PASS (CD Multi-VPS SUCCESS — sau cleanup disk vanan-shop-a).** Phase 4 — 1 công thức duy nhất (D1 net revenue). **T4.1** `LoyaltyPointsCalculator` (mới): base = `SubTotal − DiscountAmount` (bỏ VAT + phí ship) · Silo rate / Alliance VndPerPoint (default 1000) · clamp 1 nơi; `OrderWorkflowService` award dùng chung calculator (fix RC2 drift: award trước đây gồm VAT+ship). **T4.2** banner đọc điểm THỰC TẾ từ ledger (D4). **T4.3** `GET /api/loyalty/estimate` + KhachLink Checkout.razor server-estimate. Tests +16 → **Core.Tests 1733 PASS · ShopERP.Tests 99 · guard ALL PASSED · build 0 errors**. **RV:** L1 estimate (9.000 pts / clamp 0) · banner `01a0becd` = 30 thật (recompute cũ 5.500) · pending → null · L2 markers Gateway DLL + KhachLink WASM · L3/L4 Playwright 2/2 (checkout "~4545 điểm" + tracking "30 điểm"). **Infra:** vanan-shop-a disk 100% full → cleanup image prune + journal vacuum (user duyệt) → CD rerun SUCCESS. Chi tiết: Section 2 + plan §Phase 4. Batch 5 (Alliance attribution FIFO) kế tiếp.
- **Loyalty Points Integrity — Batch 3 (session 2026-09-21, `2c12852b`):** ✅ **DEPLOYED + RV PRODUCTION PASS (CD Multi-VPS SUCCESS).** Budget caps đăng ký + enforce. **T3.1** Gateway `LoyaltyConfigController`: PUT tenant config + 4 budget cap (null = unlimited) + validation (âm → 400, rateCap 0..1) → `SetBudgetCaps`; TenantConfigDto + counters; `POST /tenant/{id}/reset-counters` (daily|monthly). **T3.2** ShopERP `LoyaltyConfigApiClient` + `LoyaltyConfigAdmin.razor`: form budget (tháng/ngày/khách-ngày/trần-đơn %) + counters đã dùng + nút Reset (modal xác nhận). **T3.3** `ValcnV2_LoyaltyBudget` default ON (emergency off); OrderWorkflowService legacy path bỏ gate flag (budget luôn enforce, khớp ledger). **T3.4** verified Step 2c ledger revert + decrement. Tests: `LoyaltyBudgetServiceTests` (11 mới) + `LoyaltyConfigControllerTests` +7 → **Core.Tests 1717 PASS · build 0 errors · guard ALL PASSED**. **RV:** L1 API (PUT/GET persist, validation 400 ×2, reset daily/monthly, scope invalid 400) · L2 markers · L3/L4 Playwright app2 budget form render (0 errors) — `rv-batch3.spec.ts`. Cleanup config row xong.
- **Loyalty Points Integrity — Batch 2 (session 2026-09-20, `962fa197` + `6b9a667e` + `5f7f747d`):** ✅ **DEPLOYED + RV PRODUCTION PASS (CD Multi-VPS SUCCESS ×3).** Phase 1 (PG ledger = single source of truth) + backfill T2.3. **T1.1** `AllianceTransaction.SourceTenantId` + `SetSourceTenant` (set-once) + migration PG. **T1.2** `ILoyaltyPointLedgerService` + `LoyaltyPointLedgerService` (NEW): Award/Spend/Refund/RevertOrder/GetAwardedPoints/GetBalance — budget luôn enforce (D3) · guard tập trung theo **orderId** (RC3 — `LoyaltyIssuanceRecord` PG + legacy Silo-history fallback cho pre-cutover orders) · **tenant attribution** (RC2.4 — Silo row per (customer, tenant); **LoyaltyRewards 1:1 → 1:N** — migration drop unique `IX_LoyaltyRewards_CustomerId`, giữ unique (TenantId, CustomerId); repo `GetByCustomerAndTenantIdAsync` + `GetByCustomerIdAsync` tenant-scoped-first) · **luật Silo** (spend chỉ trừ row tenant đang tiêu — thiếu/không đủ → reject) · issuance record PG · rollback flag `LoyaltyLedgerV2` (default ON). **T1.3** `InternalLoyaltyController`: `/award /spend /refund /revert-order /balance /awarded`. **T1.4** ShopERP POS proxy cutover: `LoyaltyRewardsServiceHttpProxy` (writes → Gateway ledger, reads = SQLite mirror, IdentityLevel gate local trước forward) + `LoyaltyPointLedgerServiceHttpProxy` (D5: Gateway down → award/refund skip không fail order, spend reject); Program.cs thay `LoyaltyRewardsService`. **T1.5** `AllianceWalletService` defense-in-depth budget check+record (mọi caller); `ILoyaltyBudgetService.CheckAndAdjustPointsAsync` orderAmount nullable (skip per-order cap cho non-order). **T1.6** `OrderWorkflowService.ProcessLoyaltyPointsAsync` → ledger (legacy fallback giữ cho test scopes). RefundOrchestrationService Step 2c → ledger `RevertOrderAsync` (mode-aware, gate-safe catch). Design-time factory fix: `Npgsql.EnableLegacyTimestampBehavior` (snapshot drift — mọi `migrations add` trước đây scaffold ~200 AlterColumn giả). **Tests +19** (LedgerServiceTests 11 — attribution/luật Silo/guard/budget/reversal; proxy tests 7 — happy + Gateway-down no-throw + gate; fix gate/Redemption/Notification signatures). **Core.Tests 1699 PASS · build 0 errors · guard-check.ps1 ALL PASSED.** Migrations: PG `20260920124823` (SourceTenantId) + `20260920131530` (1:N index) · ShopERP `20260920131734` (1:N index). **RV production PASS:** L1 marker `LoyaltyPointLedger` trong Gateway DLL · **backfill SUCCESS** (24 SQLite rows → 23 created + 1 updated, +264 pts — fix FK `Customers.Id` stub upsert) · **V2a Gateway in-proc ledger**: order `01a0bf50-5349` → issuance 30 + PG row tenant `0dfab177` · **V2b ShopERP proxy**: order `01a0bf50-96ff` → issuance **2750 trên PG** (HTTP proxy → `/api/internal/loyalty/award`) · **V3 attribution**: row per (customer, tenant) đúng tenant, không merge · **L7 mirror**: shop-a SQLite (WAL) có `D3373F11`=30, `D8A40E82`=2750, `44D7C613`=2750, `B4A87A0C`=30 — khách thấy đúng điểm. **2 production bug phát hiện + fix:** backfill FK violation (23503) → upsert PG customer stub; `LoyaltySyncSubscriber` UNIQUE race khi double delivery (direct NATS + Outbox cùng xử lý → stub INSERT đụng) → retry-once fresh context (đã deploy `5f7f747d`).
- **Loyalty Points Integrity — Batch 1 (session 2026-09-20, `ebbc1f5a` + `6023ad0`):** ✅ **DEPLOYED + RV PRODUCTION PASS** (CD Multi-VPS SUCCESS ×2). Fix "điểm được tính nhưng không cộng vào khách" (RC1). `LoyaltyBalanceSyncPublisher` (subject `vanan.cloud.loyalty.changed.{deviceId}` + payload mở rộng + Outbox `LoyaltyChanged` RoutingKey) — Silo + Alliance dùng chung; `LoyaltySyncSubscriber` match (tenantId,customerId)→device, **tạo customer stub + rewards row** khi chưa có local, **max-merge** balance, history idempotent; backfill `POST /api/admin/sync/loyalty-backfill-pg` (SQLite→PG additive). **RV production:** đơn `01a0becd` (tenant "Vạn An Test", guest, complete qua **Gateway** api2) → 🎁 Awarded 30 (PG) → guard duplicate OK → shop-a: created customer stub eb5b6a96 → created row → EARN 30 → **synced balance → 30**. Trước fix: điểm PG không bao giờ tới SQLite. Core.Tests **1680 PASS** (+9) · guard ALL PASSED · build 0 errors. Plan: `docs/plans/loyalty-integrity-detail-coding-plan.md`. Batch 2 (PG ledger authority) kế tiếp.
- **Shipper feed + checkout pin map — user bug 2026-09-20 (`3495ac21` + `c9dc58df`):** ✅ **DEPLOYED + RV PRODUCTION PASS**. Đơn charity `01a0bc99` DELIVERY+confirmed nhưng shipper không thấy — **root cause: `GetNearbyOrdersAsync` tính khoảng cách shipper→SHOP (Q1), đơn giao tận Q11 cách 28.5km > radius 5km** → ẩn dù shipper đứng ngay điểm giao. **Fix (3 phần):** (1) `CommunityOrderService`: khoảng cách = shipper→**ĐIỂM GIAO** (DeliveryLat/Lng, fallback shop); `radiusKm <= 0` = "không giới hạn" (+3 tests T11-T13) · (2) `NearbyOrders.razor`: dropdown bán kính thêm **"Không giới hạn"** (value 0) + cycle 2→5→10→20→0 · (3) `Checkout.razor` + `realtime.js pinLocation`: chọn "Giao hàng" → **map ghim vị trí** (mặc định = GPS khách, kéo/thả hoặc chạm bản đồ, hidden inputs delivery-lat/lng; submit ưu tiên pin, fallback GPS D4) + nút "Dùng vị trí hiện tại". **RV:** map init (leaflet) + hidden inputs có toạ độ + realtime.js pinLocation + WASM markers ✓. Build 0 errors · Core.Tests 1672 PASS · guard ALL PASSED. CD: Gateway+KhachLink deployed; ShopERP deploy chạy (không liên quan batch).
- **Batch 2 follow-up — user feedback 2026-09-19/20 (`25447f1a` + `578dfa88`):** ✅ **DEPLOYED + RV PRODUCTION PASS**. **(1) Free/Charity đặt hàng:** bỏ bypass "Phase B" (KhachLinkLayout.GoToCheckout + Cart.razor.ProceedToCheckout — hardcode DINEIN, không phone/địa chỉ → shipper không thấy đơn vì nearby-orders chỉ nhận DELIVERY) → luôn đi qua /checkout ("Hình thức nhận hàng" + SĐT + địa chỉ + GPS). Xoá 2 hàm SubmitFreeOrderDirectAsync. **RV: free cart → "Thanh toán" dừng tại /checkout (không redirect tracking) + hiện "Hình thức nhận hàng" + notice miễn phí.** **(2) Mở rộng vùng hiển thị TẤT CẢ trang KhachLink:** `.khachlink-main` max-width 1200px + gỡ cap inline 500-700-900px ở 22 trang. **RV: container 1120px (trước 700px) tại salesman-store + wallet.** **(3) Gian hàng của tôi:** QR 120→220px (generate 240) + 3 nút Tải QR/Đồng bộ giá/Xóa xuống hàng riêng ButtonSize.Small (chờ account salesman để visual-verify). Build 0 errors · Core.Tests 1669 PASS · guard ALL PASSED. RV spec: `6_Testing/e2e-tests/rv-batch2b.spec.ts`.
- **Issue Batch #176-#181 — Batch 2 (session 2026-09-19):** ✅ **CODE DONE + DEPLOYED + RV PRODUCTION PASS** (`6ab09cbc` + `a620d1de`, CD Multi-VPS SUCCESS). Build 0 errors · Core.Tests **1669 PASS** (+10) · guard ALL PASSED. **#181** cart chrome ẩn tại `/cart` `/checkout` `/scan` — **RV: drawer home=true · /cart=0 · /checkout=0 (navCart 0/0)**. **#176** sweep `disabled=`→`Disabled` — **RV store page 0 crash errors → ISSUE CLOSED**. **#178 phần 2** "Gian hàng của tôi" (API + trang) — **RV: 401 đúng + page load OK; còn luồng salesman login**. **#179** RV: tenant `1833b55c` 0 products (2 case); fix `GetSeedCountsAsync` + error alert — **RV wizard E2E: success hiện 32/15 THẬT từ DB; SQLite verify tenant test F60B64D0 = 32 products/15 ingredients (TenantId lưu UPPERCASE — SQLite case-sensitive)**. Test tenant cleanup xong.
- **Issue Batch #176-#181 — Batch 1 (session 2026-09-19):** ✅ **FIXED + DEPLOYED + RV PRODUCTION PASS** (`49d07bd5` + `7a32a9c7`, CD Multi-VPS SUCCESS ×2). **#180** verify tenant 400 — root cause 2 lớp; fix v2 dùng explicit Vietnamese diacritics map vì **Gateway chạy `System.Globalization.Invariant=true` (runtimeconfig) → NFD/`\p{Mn}` no-op trên runtime deployed** (probe reflection chạy deployed DLL + `/proc/1/maps` + restart chứng minh platform-dependent) — **RV: tenant tên tiếng Việt "Quán Cà Phê Đậm Vị" verify không slug → 200, slug `quan-ca-phe-dam-vi-...`** · **#178** Domain `CommissionRate` 0-0.5 + UI default 0.01/1000 — **RV: POST rate=0 → 201** · **#177** client IsFree propagation (Scan.razor referral + QR legacy) — deployed, UI test L3/L4 pending. Core.Tests 1659 PASS (11 test mới) · guard ALL PASSED · build 0 errors. **RV cleanup**: 7 tenant test xóa sạch + config referral `38df680d` (COMRV1) restore active=true. Issue comments verify đã post cả 6 issues. Master plan: `docs/AI/tasks/issues_176_181/master_plan.md`.
- **Nav menu SSOT + SPA navigation (session 2026-09-19):** ✅ **COMPLETE + DEPLOYED + RV ALL PASS** (`4643c69e`). Fix menu sidebar không ổn định: trước đây 4 layout (MainLayout/AdminLayout/AccountingLayout/EInvoiceLayout) xây menu riêng → cùng role thấy menu khác nhau khi duyệt trang, và `<a href>` thuần → full reload mỗi lần bấm menu. **`ShopErpMenuService`** (mới, DI) = Single Source of Truth menu theo role, cả 4 layout gọi chung → cùng role luôn thấy cùng menu · **`VanANavigation`**: `<a href>` → `<NavLink>` (SPA navigation, hết reload) + group state ổn định (auto-expand group chứa URL hiện tại; trạng thái mở/đóng giữ khi điều hướng) · **Items thiếu bổ sung**: Owner + Tài chính (`/financial`, `/admin/business-profile`); SystemAdmin + Hàng đợi Claim, Kích hoạt Crawl, HĐĐT, Tài chính, Hướng dẫn · giữ conditional HKD/Enterprise (Sổ HKD vs Báo Cáo Tài Chính) + impersonation (Issue #103). Validation: build sln 0 errors · Core.Tests 1648 PASS · ShopERP.Tests 99/99 · Architecture 41/41 · guard ALL PASSED · CI + Accounting Tests + CD Multi-VPS SUCCESS. **RV production (app2.khachvip.online) 23/23 PASS**: Owner (adminvanan1) + SystemAdmin (sysadmin@vanan.vn) — menu đủ nhóm, bấm menu KHÔNG full reload (window marker + performance navigation entries 1→1), menu nhất quán /sitemap→/accounting→/admin/customers→/einvoice→/financial; DLL markers trên VPS (`ShopErpMenuService`, `ToggleGroup`, `IsGroupOpen`). Script: `6_Testing/rv-nav-menu.js` (+ screenshots rv-nav-*.png). L5 manual pending.
- **Realtime Platform — P6 (session 2026-09-18):** ✅ **DONE + DEPLOYED (`3fb71866`)** — E2E + RV + reuse guide. **E2E 12/12 PASS trên production** (`realtime-shop-chat.spec.ts` 7/7 + `realtime-tracking.spec.ts` 5/5, chạy qua `realtime-rv.config.ts`). **RV L1** API contracts · **L2** RCL assets 200 trên 3 host (diemthuong2 / api2/shoperp / timlathay) · **L3** Playwright UI (store chat send end-to-end) · L4/L5 manual pending. **2 bug production bị E2E bắt:** (1) Shop chat chicken-and-egg → fix = public widget: ensure create-only trước authorize + history filter per-caller + per-user SignalR group (khách không thấy tin khách khác; staff thấy toàn bộ) · (2) **Razor string-param binding**: `Param="_field"` không `@` = literal string (chỉ Guid/double thành expression) → `CustomerToken="_customerToken"` gửi literal → 401; ChatPanel/LeafletMap shim (P4) cũng dính — order chat logged-in bị 401 từ P4 deploy, fix bind `@` + verify 0 literal trong WASM. **Reuse guide** đã thêm §Realtime vào `docs/UI_Platform_Implementation_Guide.md`. Build 0 errors · guard ALL PASSED · 68/68 Realtime tests · Core.Tests 1647 PASS.
- **Realtime Platform — P5 (session 2026-09-18):** ✅ **DEPLOYED (`f05960c5`)** — consumer đầu tiên = **Shop chat**. Server: `ShopRealtimeAuthorizer` (keyed Shop — staff `tenant_id` claim == SubjectId ∨ initiator ∨ participant) · `IRealtimeParticipantAuthorizer` + tenant-aware overload · `RealtimeIdentity.TenantId` (StaffJwtValidator parse `tenant_id`) · `RealtimeController` ensure conversation Shop (initiator = caller, counterpart = shop) + staff `EnsureParticipantAsync` trước send + **`GET /api/realtime/shop/conversations`** (inbox: staff-only, list + last-message preview + customer name). **KhachLink `/store/{slug}`:** F8 device id tạo ngay trên trang (không đợi Checkout) + section chat `RealtimeChatPanel SubjectType="Shop"` · **F9:** GoogleMaps.razor viết lại = `VanAnMap` tĩnh + khoảng cách haversine ("Cách bạn ~X km"), bỏ `<style>` inline (F11). **UI.Platform:** staffToken param (HTTP Bearer + SignalR `?access_token=`) ở `IRealtimeChatClient`/`ILiveLocationClient`/`RealtimeHttpAdapter`/`RealtimeChatPanel` (guests giờ cũng SignalR — F3). **ShopERP:** `ShopInboxApiClient` (Owner JWT) + trang `/community/messages` (AdminLayout, [Authorize Owner], list + RealtimeChatPanel StaffToken) + nav link + App.razor RCL scripts. Build 0 errors · 66/66 Realtime tests · Core.Tests 1645 PASS. Chi tiết: task card Section 18.9.
- **Realtime Platform — P4 (session 2026-09-18):** ✅ **DEPLOYED (2026-09-18, `d8072ec5`)**. UI Platform extraction: `IRealtimeEndpointProvider` (F4 — derive Gateway URL từ app origin, port `DeriveGatewayUrl` C8) + `IRealtimeChatClient`/`ILiveLocationClient`/`IMapJsAdapter` + `RealtimeHttpAdapter` (named client `realtime`, absolute URLs — hết vấn đề WASM-vs-Server scoping) + `LeafletMapAdapter` + `RealtimeChatPanel`/`VanAnMap` + `wwwroot/js/realtime.js` + vendored Leaflet (served tại `_content/VanAn.UI.Platform/...`). **KhachLink migrated:** ChatPanel/LeafletMap thành shim · OrderTracking dùng `VanAnMap` + `ILiveLocationClient.GetLatestAsync` + join `/hubs/tracking` (ping push live, legacy hub giữ cho status) · DeliveryTracking ping qua `RecordPingAsync` · ChatHttpService xoá · index.html trỏ RCL assets. **Gateway compat:** `RealtimeController` fallback `Order → IChatService.GetOrCreateConversationAsync` khi conversation chưa tồn tại (đơn mới không 404). `AddRealtimePlatform()` 3 host (KhachLink/ShopERP/Directory). **F11:** VanAInput. Build 0 errors · guard ALL PASSED · 56/56 Realtime tests · Core.Tests 1635 PASS. **CI ✅ · Accounting Tests ✅ · CD Multi-VPS ✅ (deploy 3 VPS + smoke test).** **RV L2:** `_content/VanAn.UI.Platform/js/realtime.js` 200 (6KB, chứa `vananMap`/`vananRealtime`) · `lib/leaflet/leaflet.{js,css}` 200 · index.html deployed trỏ RCL assets, không còn `/js/leaflet.js`.
- **Realtime Platform — P3 (session 2026-09-17):** ✅ **DEPLOYED (2026-09-18, `d8072ec5`)**. Gateway realtime layer: `MessagingHub` `/hubs/messaging` + `TrackingHub` `/hubs/tracking` (group `msg_`/`loc_{subjectType}_{subjectId}`, join qua keyed `IRealtimeParticipantAuthorizer`, **default deny** khi subject chưa đăng ký authorizer) · `RealtimeController` `/api/realtime/*` (send/history/ping/latest — không còn role Shipper check như `api/community/*`, nên buyer tự xem được toạ độ đơn mình = GW-8) · `IRealtimeTokenValidator` × 3 + `RealtimeIdentityResolver` (Customer→Device→Staff) · `IRealtimeSubjectResolver` (subject→TenantId, unknown→null→400, không đoán tenant) · **F3 FIXED**: validators đọc credential từ **query string trước header** → guest mở được SignalR bằng `?customerDeviceId=` (trước chỉ HTTP polling) · **GW-6**: `/hubs/chat` + `/hubs/location` giữ nguyên route/group/tên method, thân hàm delegate sang auth + access dùng chung (bỏ bản sao logic). Build full sln 0 errors · guard ALL CHECKS PASSED · 37/37 Realtime tests PASS (11 P2 + 26 P3). `RealtimeController` thêm vào exemption W12-G7 (customer/guest-facing, `[Authorize]` sẽ chặn guest trước khi endpoint chạy — cùng precedent `CommunityController`).
- **Accounting revenue-loss fix (2026-09-17/18):** ✅ Root cause verified on production (tenant "Vạn An Cafe (HKD Group 1)"): 5 orders paid within ~4 min, only 1 got revenue entries. **C-1 duplicate detection** (`CheckDuplicateEntryAsync`) matched (tenant, type, amount, accountCode) in 5-min window WITHOUT order reference → distinct same-amount orders blocked as duplicates; exception swallowed in `ConfirmPaymentAsync` → Paid orders without entries. Fixes (`236d1235` + `f6e94108`, deployed + CD SUCCESS): duplicate check by reference · `GenerateAccountingEntriesAsync` idempotent via `ExistsByReferenceAsync` · `SimpleAccountingEventHandler` NATS subject `vanan.shoperp.order.completed` (+ legacy) — completion-accounting path was dead (subject mismatch) · audit flag fail-safe in background scopes. **Data repair DONE**: re-published `OrderPaymentConfirmed` NATS events → 5 orders now have full entries (511+3331+632) + HKD JournalEntries; `01a0afac` untouched. Core.Tests 1616 pass · guard ALL PASSED.
- **Branch:** `main` @ `6ab09cbc` (Issue Batch #176-#181 Batch 2 CODE DONE — cart chrome #181 + salesman store #178-ph2 + quick-setup truth #179 + sweep #176; chờ push/deploy/RV. Batch 1 FIXED + DEPLOYED + RV PASS `49d07bd5` + `7a32a9c7`. Nav menu SSOT + SPA navigation DEPLOYED + RV ALL PASS. Realtime Platform P3-P6 DEPLOYED + CD SUCCESS. Accounting revenue-loss fix + audit fail-safe deployed. Realtime Platform P2 — generic Domain/services + PG migration. P1b GPS map + Free/Charity checkout. Community Commerce Batch 1 + 2 + Chat + GPS + referral QR + commission base/self-referral. KhachLink Profile Sprint 1+2. GTM W2 + currency fix. W1 Merchant Audit + nginx Directory SSR. R2.2 Reseller Accounting — PR #169. Crawl-to-Onboard 8 phases. Issue #103/#157/#161/#156 deployed).
- **Build full sln:** 0 errors · **CI:** PASS · **.NET SDK:** 8.0.422
- **Realtime Platform — P2 (session 2026-09-17):** ✅ **P2 DONE + DEPLOYED + RV L1 PASS** @ `2c3e0360`. Generic Domain + services + PG migration `20260917101938_AddRealtimePlatformP2` (backfill `SubjectId` trước unique index). 2 blocker từ review đã fix: **F2** unique index `OrderId` → `(TenantId, SubjectType, SubjectId)`; **F6** index ping generic. `IChatService`/`ChatService` giữ nguyên (không regression). Build 0 errors · guard ALL PASSED · 11/11 test mới (`RealtimePlatformP2Tests`) · 248/248 Community regression · CI + Accounting Tests + CD Multi-VPS SUCCESS. **RV L1:** PG có `ConversationParticipants` + cột `SubjectType/SubjectId/TrackerId`; unique index `IX_Conversations_TenantId_SubjectType_SubjectId` tạo đúng; index `OrderId` đã thành non-unique; **backfill 7/7 conversations + 40/40 pings** (không còn giá trị 0) → migration an toàn trên dữ liệu production. Review P2-P6: 11 findings (F1-F11) — **F1: Logistics/JobMarket chỉ là preset nav-flags, không phải module → P5 đổi thành Shop chat**. Quyết định: GPS trang shop = map tĩnh + khoảng cách; phía shop = trang inbox `/community/messages`.
- **Realtime Platform (session 2026-09-17):** 🚧 **P1 DONE + DEPLOYED + RV PASS (L1-L4)** @ `b12a99d2`. Root-cause RV: chat + GPS vẫn chết trên UI dù fix `26dc9e62` deploy. 6 defect + 8 coupling. **P1 fixes** (D1 Google customer sync · D2/D3 buyer tracking endpoint + map · D4 checkout coords · D5 GPS resume · D6 guest chat/tracking via device id). Build full sln 0 errors · 40/40 chat+delivery tests · CI ALL PASSED · CD Multi-VPS SUCCESS. **RV:** L1 API (401/404/200/403 + guest send) ✅ · L2 WASM markers + ShopERP redeployed ✅ · L3 Playwright guest UI (chat panel + map + no errors) ✅ · L4 guest send flow ✅ · L5 manual pending. Docs guest chat/tracking đã sửa. **P2-P6 pending** (generic Domain/services/hubs + UI Platform extraction + Logistics/JobMarket consumers). Task card: `docs/AI/tasks/realtime_platform/task_card_01_realtime_chat_gps_reusable.md`.
- **Community Commerce Full Flow (session 2026-09-16):** ✅ Batch 1 (Issues #1-4) + Batch 2 (4 RV defects) + Chat fix + Floating cart + Issue #175 ALL COMPLETE + DEPLOYED + RV PASS. Full chain works end-to-end: salesman QR → customer checkout (DELIVERY + CustomerId) → owner confirm via Gateway → shipper accept/pickup/delivering/delivered → order completed + Outbox events + NATS sync → chat customer↔shipper → loyalty points awarded → public tracking works. See Section 2.
- **Commission base + self-referral (session 2026-09-17):** ✅ per-product commission base + self-referral blocking — DEPLOYED + RV PASS (`05443115`). HEAD=`05443115`.
- **Salesman Referral QR (session 2026-09-16/17):** ✅ correct domain + scan-to-buy + commission — DEPLOYED + RV PASS (`aceab325`).
- **Chat + GPS + Salesman QR (session 2026-09-16):** ✅ 3 defects fixed + DEPLOYED + RV PASS (`26dc9e62`). Chat token literal (`@` prefix), GPS DeliveryTaskId, QR firstRender timing.
- **KhachLink UX Fixes (session 2026-09-16):** ✅ Fix 1 NearbyProducts GPS error message (`32d0bae8`) — DEPLOYED + RV PASS (pwa.js + NearbyProducts.razor). Fix 2 Home default search mode = "Tìm sản phẩm" (`a6e712e3`) — DEPLOYED + CD PASS.
- **KhachLink Profile Transition Sprint 3:** ✅ CODE COMPLETE on `feature/khachlink-sprint3-audit-sw` @ `d29e621b`. Pending: push → PR → merge → CD → RV Layer 1-5.
- **Financial Intelligence MVP-2:** ✅ All 5 phases complete on feature branch (61/61 tests PASS). Pending: push + PR + CD + RV.
- **Tech debt:** TD-MVPS-001→004, TD-CUSTSYNC-001, TD-ASYNCDP-001, TD-GCP-001, TD-NETDASH-001, TD-OCR-01→05

---

## 4. Next Actions

**Loyalty Points Integrity — BATCH 4 (✅ DEPLOYED + RV PRODUCTION PASS 2026-09-21 — `5e9ada23`, CD Multi-VPS SUCCESS):**
- [x] T4.1 `LoyaltyPointsCalculator` (D1 net revenue) + award path OrderWorkflowService dùng chung formula (fix RC2)
- [x] T4.2 banner đọc điểm THỰC TẾ từ ledger (`GetAwardedPointsAsync`) — bỏ recompute (D4)
- [x] T4.3 `GET /api/loyalty/estimate` (server) + KhachLink Checkout.razor gọi endpoint
- [x] RV: L1 API (estimate 9.000/clamp 0 · banner 30 thật · pending null) · L2 markers · L3/L4 Playwright 2/2 (`rv-batch4.spec.ts`)
- [x] **Batch 5 (`c1db4f6a` + `1b810476`):** ✅ DEPLOYED + RV PRODUCTION PASS — T5.1 Alliance attribution (D2) + T5.2 settlement; V7 E2E production PASS (2 REDEEM entries B→B/A→B đúng, settlement đúng, cleanup pristine); L3 regression 3/3 PASS
- [x] **BUG-1 + BUG-1b (`fac4e979` + `693f2763`):** ✅ FIXED + DEPLOYED + RV PRODUCTION PASS — sync payload mang real customerId (mirror stub tạo đúng identity PG) + subscriber overwrite PG authority (spend sync giảm mirror, "synced balance → 70" verify production)
- [ ] **Finding nhỏ (defer):** sync events có thể xử lý lệch thứ tự (double-delivery) → mirror tạm stale giữa các event (display-only, tự hội tụ). Fix đúng cần monotonic sequence + migration — làm khi Alliance có production thật.

**Loyalty Points Integrity — BATCH 3 (✅ DEPLOYED + RV PRODUCTION PASS 2026-09-21 — `2c12852b`, CD Multi-VPS SUCCESS):**
- [x] T3.1-T3.4 budget caps (API PUT 4 cap + validation + reset-counters; UI form + counters + Reset; flag `ValcnV2_LoyaltyBudget` default ON; Step 2c verified)
- [x] RV: L1 API PASS · L2 markers PASS · L3/L4 Playwright budget form render PASS (`rv-batch3.spec.ts`)

**Loyalty Points Integrity — BATCH 2 (✅ DEPLOYED + RV PRODUCTION PASS 2026-09-20 — `962fa197`/`6b9a667e`/`5f7f747d`):**
- [x] Push → CD Multi-VPS SUCCESS ×3 (migrations PG `20260920124823`/`20260920131530` + ShopERP `20260920131734`)
- [x] Backfill `POST /api/admin/sync/loyalty-backfill-pg` → SUCCESS (23 created + 1 updated, 264 pts)
- [x] **RV:** V2a Gateway ledger issuance 30 · V2b ShopERP proxy issuance 2750 PG · V3 attribution row/tenant · L7 mirror SQLite 4 row đúng balance · V8 reversal — unit-tested (LED-9), UI cancel flow chưa test production (flag ValcnV2_RefundReversal OFF — defer)
- [ ] **RV còn lại (có thể defer):** luật Silo reject qua UI redemption (unit LED-2/LED-3 cover; cần customer token) · V5 banner thực tế (Batch 4 khi banner chuyển sang GetAwardedPointsAsync)

**Issue Batch #176-#181 — BATCH 2 (✅ CODE DONE `6ab09cbc` — chờ deploy + RV):**
- [x] **#181** (P2): ẩn floating CartDrawer + header cart icon tại `/cart`, `/checkout`, `/scan` — `HideCartChrome` (route check) + `Navigation.LocationChanged` re-render. Task card: `task_card_181_order_flow_cart_duplication.md`.
- [x] **#178 phần 2** (P2, feature): trang "Gian hàng của tôi" `/community/salesman-store` + API `GET/POST /api/community/salesman/products` (+add/remove) — QR composite deterministic, giá live từ catalog. Task card: `task_card_178_referral_config_qr_list.md`.
- [x] **#179** (P2): RV production xong (0 products + 0 Tenants row cho 1833b55c; log không có ApplyTemplate — lần chạy user trước redeploy) → fix: `GetSeedCountsAsync` đọc DB thật + QuickSetup hiển thị lỗi đỏ, không fake success. Task card: `task_card_179_quick_setup_seed.md`.
- [x] **#176** (P3): sweep lowercase `disabled` → `Disabled` 5 chỗ (grep 0 match) — còn RV store page để đóng issue. Task card: `task_card_176_vanan_button_disabled_crash.md`.
- [x] **RV batch 2 (2026-09-19, sau deploy `a620d1de`):** #179 wizard E2E → success hiện 32/15 THẬT + SQLite verify (F60B64D0 = 32 products/15 ingredients, TenantId UPPERCASE) ✓ · #176 diemthuong2 store page 0 crash errors → **issue closed** ✓ · #181 /cart + /checkout 0 drawer ✓ · #178 API 401 + page load ✓ (còn: luồng salesman login đầy đủ — chờ tài khoản salesman).
- [ ] **#177** UI test L3/L4: đặt mua free/charity qua 3 path (store page, product QR, referral QR) — C2/C3 UI đúng (ẩn payment selector, bước donation).

**Realtime Platform — P1b follow-up (✅ FIXED + DEPLOYED + RV PASS — 2026-09-17, `96e733a6`):**
Manual test sau P1: chat OK nhưng (a) GPS/map không hoạt động cả 2 phía, (b) sản phẩm Free/Charity không tạo được order.
- **D7** Shipper không thấy map: `DeliveryTracking` lấy toạ độ từ `nearby-orders` — endpoint này **loại trừ đơn đã có DeliveryTask active** (chính đơn shipper đang xem) → không có toạ độ → `_showMap=false`. Fix: dùng `/api/community/orders/{id}/tracking`.
- **D8** Map khách trắng/thiếu pin shipper: toạ độ mặc định `0` vẫn qua `HasValue` → map tâm (0,0); marker chỉ add ở lần render đầu → ping shipper không cập nhật. Fix: endpoint chuẩn hoá `0`→null; client yêu cầu toạ độ thực + center shop→delivery→shipper; `LeafletMap` upsert marker mỗi render (`leafletMap.upsertMarker`).
- **D9** Free/Charity không tạo được order: ShopERP product API không có `productType` → cart `IsFree=false` → Gateway Tier 0 chặn giá 0. Fix: Gateway tự resolve Free/Charity từ `FeaturedProducts` trước price guard (authoritative) + `Store.razor` enrich ProductType từ featured catalog.

**Realtime Platform (🚧 APPROVED + IMPLEMENTING — session 2026-09-17):**
Task card: `docs/AI/tasks/realtime_platform/task_card_01_realtime_chat_gps_reusable.md`
- [ ] **P0** — verify production (log `"falling back to guest mode"` + query PG `Customers`/`Orders`) — không cần chờ gì
- [x] **P1** — fix production blockers ✅ DONE + DEPLOYED + RV PASS (L1-L4) `b12a99d2`: D1 customer sync (`SocialAuthController` enqueue `CustomerCreated`) · D2/D3 buyer-accessible `GET /api/community/orders/{id}/tracking` + map render · D4 checkout toạ độ · D5 GPS resume on load · D6 guest chat/tracking via `X-Customer-Device-Id`. Build 0 errors · 40/40 tests · CI ALL PASSED · CD SUCCESS · RV L1-L4 PASS (L5 manual pending).
- [x] **P2** — ✅ DONE + DEPLOYED + RV L1 PASS (2026-09-17, `2c3e0360`): Domain additive (`RealtimeSubjectType` + `Conversation.SubjectType/SubjectId` + `ConversationParticipant` + `DeliveryTracking.Subject*`) · `IRealtimeMessagingService`/`ILiveLocationService`/`IRealtimeParticipantAuthorizer` + `OrderRealtimeAuthorizer` (keyed DI) · migration PG `20260917101938_AddRealtimePlatformP2` (backfill `SubjectId`) · **fix F2** (unique index `(TenantId, SubjectType, SubjectId)`) + **fix F6** (tracking subject index). Build 0 errors · guard ALL PASSED · 11/11 test mới · 248/248 Community regression.
- [x] **P3** — ✅ **DEPLOYED (2026-09-18, `d8072ec5`)** 2026-09-17: Gateway `MessagingHub` (`/hubs/messaging`) + `TrackingHub` (`/hubs/tracking`) + `RealtimeController` (`/api/realtime/conversations/messages` · `GET /conversations/{type}/{id}` · `POST /location/ping` · `GET /location/{type}/{id}/latest`) + `IRealtimeTokenValidator` × 3 (`CustomerTokenValidator`/`DeviceTokenValidator`/`StaffJwtValidator`) + `RealtimeIdentityResolver` (Customer→Device→Staff) + `IRealtimeSubjectResolver` (subject→TenantId) + `RealtimeAuthorizerLookup` (**default deny**) + **F3 fixed** (device token đọc từ query string → guest mở được SignalR, cả hub mới lẫn `/hubs/chat` + `/hubs/location` cũ) + **GW-6** (hubs cũ giữ nguyên route/group/tên method, chỉ delegate auth + access check). Build full sln 0 errors · guard ALL PASSED · 37/37 Realtime tests (11 P2 + 26 P3). Chi tiết: task card Section 18.7.
- [x] **P4** — ✅ **DEPLOYED (2026-09-18, `d8072ec5`)** 2026-09-18: UI-1..UI-13 + **F4** (`IRealtimeEndpointProvider` + RCL wwwroot `realtime.js`/Leaflet, verify qua publish + production L2) + **F11** (VanAInput). KhachLink migrated to generic surface (shims, OrderTracking `GetLatestAsync` + `/hubs/tracking`, DeliveryTracking `RecordPingAsync`, ChatHttpService xoá). Gateway: `RealtimeController` fallback Order→legacy ensure (đơn mới không 404). `AddRealtimePlatform()` 3 host. Build 0 errors · guard ALL PASSED · 56/56 Realtime tests · Core.Tests 1635 PASS. Chi tiết: task card Section 18.8.
- [x] **P5** — ✅ **DEPLOYED (`f05960c5`)** 2026-09-18: Shop chat + map tĩnh + inbox (xem Section 3). Logistics/JobMarket → **P7** (chưa tồn tại — F1). Chi tiết: task card Section 18.9.
- [x] **P6** — ✅ **DONE + DEPLOYED (`3fb71866`)** 2026-09-18: E2E 12/12 PASS + RV L1-L3 + reuse guide §Realtime. 2 bug production fix (public widget model + Razor string binding). L4/L5 manual pending. Chi tiết: task card Section 18.10.
- [ ] **P7 (deferred)** — Logistics/JobMarket consumers — chỉ khi Sprint 8/9 thành module thật (entity `Shipment`/`JobApplication`)
- ✅ Docs guest chat/tracking đã sửa: `07-customer.md` (§2.1, §2.4, §5.1, §6.1, FAQ), `04-shipper.md` (§8.3), `README.md` (§3.3)

**Completed groups (Salesman Referral QR · Chat + GPS + QR · KhachLink UX Fixes · Community Commerce Full Flow)**
moved to `docs/AI/project_state_archive.md` — see also Section 10.

**KhachLink Profile Transition Sprint 3 (pending push/PR/RV):**
- Push branch `feature/khachlink-sprint3-audit-sw` → `gh pr create` → merge → CD Multi-VPS deploy
- RV Layer 1-5 on production (timlathay.com + diemthuong2.khachvip.online + app2.khachvip.online):
  - L1 API: `GET /api/audit-trail/entity/12/{instanceId}` + `GET /api/audit-trail/recent?count=10`
  - L2 Static: `GET /js/onboarding-tour.js` → verify `vananTriggerSWUpdate` present
  - L3 Playwright: `npx playwright test profile-transition` → Sprint 3 tests PASS
  - L4 UI flow: admin change profile → audit row (chờ ~5-10s async flush) · `/admin/audit-trail` summary cards · failed login → FailedLogin row · rate limit → RateLimitHit row · toggle RV (tắt Audit_KhachLink → không row · bật lại → row) · accounting sync RV (tạo bút toán → audit row NGAY)
  - L5 Manual: KhachLink diemthuong2 → change profile → reopen → SW update triggered + nav updated
- Manual RV Sprint 2 gaps: admin change diemthuong2 FullCommerce→Directory (cart modal) + Directory→FullCommerce (onboarding tour)

**GTM Drill Machine MVP (✅ W1 + W2 COMPLETE — W3 NEXT):**
- Next: W3 Revenue Proof (D1 domain mod — counters on tenant GrowthDashboard) → W4 Referral (D2) → W5 consent + flag `GrowthMachine:Enabled` + deploy + RV
- DEFER theo Gates: AI SDR/auto scoring (G1 — 0 telemetry), search counter, sitemap toàn site (BOM #4 T2), payment gateway (G2)

**Financial Intelligence MVP-2 (active):**
1. Push branch `feature/financial-intelligence-mvp2` (await user approval)
2. `gh pr create` → merge → CD Multi-VPS deploy
3. RV L1-L5

**Pending manual RV (carry-over):**
- Crawler post-deploy RV (commit `942467e0`): trigger crawl → verify >20 Pending tenants + filters applied
- Post-deploy RV (commits `e7848be9`, `1eeb4615`, `6c9182da`, `5c5a07c5`): RV timlathay.com (Blazor web.js + WebSocket + markers) + diemthuong2 (ThemeType + SW v19) + Issue #161 (accounting date) + Issue #156 (nav groups)
- Issue closure: #130 (Guard QR creation) + #126 (Guard QR Verify) — pending VPS RV + close

**Deferred / monitoring:**
- R2 (S4 EasyOCR) — deferred until VPS upgrade (4GB RAM) + tenant demand
- GCP Data Seeding — seed production data (fresh DB only 3 test tenants)
- #99-3 Phase B — Alliance VND Normalization (awaiting user approval)
- Hybrid Strategy Bước 2 — trigger when CPU > 70% / Memory > 80%
- Post-Sprint 7 flaky tests — 4 EInvoiceOrchestratorTests (skipped via CI filter)
- v3.0 deferred — INV-009, payment provider (VNPay/Momo), Ops Cost, Tier Distribution
- nginx deferred task cards — per-user rate limit, Blazor API aggregation, API classification
- R2.2 follow-up: Configure `PlatformAccountingTenantId` SystemSetting in production DB (1-time SysAdmin setup)

---

## 5. Active Architecture Decisions

| Decision | Lý do |
|---|---|
| Gateway = Order Creator + Routed Async Delivery (Option C) | Multi-VPS support, PG source of truth, NATS routed by ShopInstanceId |
| CoreHub = in-process background service trong Gateway | Monolith Phase 1-2 |
| ShopERP = SQLite (Business) + PostgreSQL (Accounting) | ADR-001: accounting always online |
| `AccountingEntry` immutable, Reversal Entry | Audit trail bắt khu xâm phạm |
| Multi-tenancy `TenantId` filter mọi layer | Data isolation per HKD |
| Loyalty Alliance = Option B (HTTP proxy + cache + idempotency) | Multi-VPS ready, ShopERP does NOT connect to PG directly |
| nginx 5-layer rate limit | Separate API/page/auth/WebSocket/static quotas — prevents 503 on fast navigation |
| Directory SSR = separate container, nginx map-based routing | Directory-profile tenants get <1s SSR load; Commerce domains keep WASM |

**Deployment Modes:** SaaS (`docker-compose.prod.yml` — all on 1 VPS) ‖ Edge (`docker-compose.edge.yml` — Server A: ShopERP+SQLite+NATS, Server B: Gateway+PG+KhachLink).

---

## 6. History Log (compressed — see archive + git log)

* [2026-09-17] **COMMISSION BASE = REFERRED PRODUCT + SELF-REFERRAL BLOCKING (`05443115`).** ✅ COMPLETE + DEPLOYED + RV PASS. Per-product commission base (referred line SubTotal, pre-VAT, no shipping) via `ReferralCommissionCalculator` (single source of truth) + `SalesReferral.CommissionBaseAmount` + migration `20260917011937`; `WalletService` uses the same calculator. Self-referral blocked at checkout + at commission time (CustomerId / fingerprint / device token / `Order.CustomerDeviceId` for guests) → `RiskScoreInput.SelfReferral` weight 100 → Rejected + `FraudFlag(SelfDeal)`. RV: base 50000/commission 1500 (not 8250); forced buyer==salesman → Rejected RiskScore 100; commissions API pending 3150 / rejected 1500. Build 0 errors · Guard ALL PASSED · 50/50 tests · CD Multi-VPS SUCCESS. Task card: `task_card_07_commission_base_selfreferral.md`.
* [2026-09-18] **ACCOUNTING REVENUE-LOSS FIX + DATA REPAIR.** Production bug (tenant "Vạn An Cafe (HKD Group 1)"): 5 đơn hoàn tất + Paid nhưng chỉ 1 ghi nhận doanh thu. Root cause (verify production SQLite+WAL+PG): C-1 `CheckDuplicateEntryAsync` chặn nhầm đơn khác nhau cùng số tiền trong 5 phút (không so Reference) + exception bị nuốt ở `ConfirmPaymentAsync`; NATS subject `SimpleAccountingEventHandler` sai (`ordercompleted` vs `order.completed`). Fix: duplicate check theo reference · idempotency `ExistsByReferenceAsync` · subscribe đúng subject (+ legacy) · audit flag fail-safe background scope. Deployed `236d1235` + `f6e94108` (CD SUCCESS ×2). Repair: re-publish `OrderPaymentConfirmed` NATS → 5 đơn đủ entry (511/3331/632) + JournalEntries, `01a0afac` nguyên vẹn, revenue T1 09/2026 = 1.309.000đ. Tests 1616 pass · guard ALL PASSED.
* [2026-09-17] **REALTIME PLATFORM — ROOT-CAUSE ANALYSIS + TASK CARD.** Chat + GPS vẫn chết trên UI dù fix `26dc9e62` deploy. Tìm ra 6 defect (D1 khách Google OAuth không sync PG → `Order.CustomerId=null` → chat/GPS chết; D2/D3 map khách dùng endpoint shipper-only 403 + không set `_showMap`; D4 checkout không gửi toạ độ; D5 GPS mất khi reload; D6 guest không chat/tracking) + 8 coupling (C1-C8) chặn tái sử dụng. Task card `docs/AI/tasks/realtime_platform/task_card_01_realtime_chat_gps_reusable.md` (approved). Quyết định: guest CÓ chat+tracking (`X-Customer-Device-Id`); Domain additive approved; consumer = Logistics + JobMarket. Docs guest đã sửa. **Chưa implement.**
* [2026-09-15] **COMMUNITY COMMERCE ISSUES #1-4 COMPLETE.** Issue #1 OrderType DELIVERY (`26b060ee`): `Order.SetOrderType()` + CreateOrderCommand + Checkout.razor selector. Issue #2 NATS sync (`369b2986`): `OrderSyncSubscriber` read OrderType from payload + `OrderService` add delivery fields to Outbox event. Issue #3 GPS mock (`5b97bcf9`): shared `gps-mock.ts` helper injected into 3 e2e specs. Issue #4 X-Dev-OTP gate: removed X-Dev-OTP from `/otp/send` + `/upgrade/send-otp` + added `POST /api/customer-identity/dev-token` (secret-gated). Full chain: checkout → DELIVERY order → NATS sync → SQLite → owner confirm → shipper sees order. See Section 2.
* [2026-09-14] **CHARITY CHECKOUT FLOW C1-C3 + 3 COMMUNITY COMMERCE BUGS.** Charity: `9b7d0c8e` + `f505a242` (ExecuteAtomicAsync + AllItemsFree + Charity_Donation_Enabled). Community: `f39c8649` + `88f3496f` + `6fe17d31` (DeliveryTracking route + GPS-optional + gateway HttpClient).
* **Older (2026-09-13 and before):** KhachLink Profile Transition Sprint 1-3, GTM W1-W2, R2.2 Reseller Accounting, Crawl-to-Onboard 8 phases, Directory SSR, Issue #103/#156/#161/#157, Financial Intelligence MVP-2, OCR Hub R1, Dynamic CORS, Multi-VPS Option C. See `docs/AI/project_state_archive.md`.

---

## 7. Active Files Reference

| File | Role |
|---|---|
| `docs/AI/tasks/realtime_platform/task_card_01_realtime_chat_gps_reusable.md` | **Realtime Platform** — chat + live location reusable (approved, P0-P6) |
| `docs/AI/tasks/community_commerce_fixes/` | Community Commerce master plan + 4 task cards (Issues #1-3 COMPLETE, #4 DEFER) |
| `docs/AI/tasks/khachlink_profile_transition_ux/` | KhachLink Profile Transition UX (Sprint 1+2 deployed, Sprint 3 pending push) |
| `docs/AI/tasks/gtm_drill_mvp/` | GTM Drill Machine MVP (W1-W2 COMPLETE, W3-W5 planned) |
| `docs/AI/tasks/task_financial_intelligence_mvp2.md` | Financial Intelligence MVP-2 task card (5 phases complete, pending PR) |
| `docs/Architecture/ADR001-Station-Architecture.md` | ADR-001 v3 (Option C) |
| `docs/AI/plans/ecosystem-master-business-model.md` | Ecosystem Master Business Model v1.0 |
| `docs/AI/project_state_archive.md` | Archived history (2026-07-24 + 2026-08-03 + 2026-08-09 + 2026-08-23 + 2026-09-06 + 2026-09-15) |

---

## 8. Architecture Quick Reference

```
=== SaaS Mode (docker-compose.prod.yml) ===
KhachLink WASM/SSR (5002) → Gateway (5001) → ShopERP (5003) → SQLite (local)
                       ↓
              [in-process CoreHub]
                       ↓
                  PostgreSQL (central)

=== Edge Mode (docker-compose.edge.yml) ===
Server A (Edge):              Server B (Central):
  ShopERP → SQLite              Gateway → PostgreSQL
  NATS sync worker              [in-process CoreHub]
       ↓ NATS ↓
  ---------------→ Gateway
                   KhachLink → Gateway (HTTP)
```

**Auth:** Cookie (Blazor Server) + JWT Bearer (API). `DevLoginController` (`#if DEBUG`) for E2E.
**Roles:** `UserRole` (tenant-scoped) + `PlatformRole` (cross-tenant: SystemAdmin).

---

## 9. AI Health Check

- **Assumptions:** 0
- **Verified Facts:** Branch=`main` @ `693f2763` (Loyalty Points Integrity BUG-1 + BUG-1b FIXED + DEPLOYED + RV PRODUCTION PASS 2026-09-21 — CD Multi-VPS SUCCESS ×2; E2E D2 production verify: mirror stub customer Id = test customerId (cùng identity PG) + LoyaltyRewards row balance 100→150→70 sau spend + history EARN/EARN/REDEEM đúng + "synced balance → 70" — cả sync payload customerId lẫn mirror overwrite hoạt động production; cleanup PG + SQLite pristine. Batch 5 `1b810476` DEPLOYED + RV PASS: V7 attribution FIFO production (2 REDEEM B→B/A→B đúng, settlement A=100/30/30/70 + B=50/50/0/80/0, idempotency safe). Core.Tests 1745 PASS · ShopERP.Tests 99 · guard ALL PASSED · build 0 errors. Batch 4 `5e9ada23` DEPLOYED + RV PASS. Batch 3 `2c12852b` DEPLOYED + RV PASS. Batch 2 DEPLOYED + RV PASS. Batch 1 DEPLOYED + RV PASS. Nav menu SSOT RV 23/23 PASS. Realtime Platform P6 `3fb71866` (12/12 E2E + reuse guide). Realtime Platform P2/P3 (2026-09-17): generic hubs + validators (query-string-first — guest SignalR OK). `KhachLinkNavFlags.cs:52-53` vẫn `TODO R3` (Logistics/JobMarket chưa có entity — P7). Security: `.devin/rules/dev-token-secret.md` — `DevToken__Secret` unset (endpoint 404).
- **Open Questions:** 6 (PlatformAccountingTenantId not configured · Turnstile keys not configured · Rate limit uses container IP not forwarded client IP · Sprint 1 RV partial — Directory-profile WASM domain needed for P2.2/P2.3 RV · Realtime Platform P0 production verification not yet run — D1 needs Gateway log/DB confirmation · Sync events ordering (double-delivery) có thể làm mirror tạm stale — display-only, defer)
- **Gate 6 Status:** ✅ Assumptions (0) < Verified Facts (35+), Open Questions (6) → CLEAR

---

## 10. Maintenance Log

> Full historical maintenance log (pre-2026-09-17): see `docs/AI/project_state_archive.md`.
> **Last Updated:** 2026-09-21 · **Branch:** `main` @ `693f2763` (Loyalty Points Integrity — Batch 5 + BUG-1/BUG-1b fixes — DEPLOYED + RV PRODUCTION PASS)

* **2026-09-21 — LOYALTY POINTS INTEGRITY BUG-1 + BUG-1b FIXED + DEPLOYED + RV PRODUCTION PASS (`fac4e979` + `693f2763`, CD Multi-VPS SUCCESS ×2).** Fix 2 bug RV bắt từ Batch 5. **BUG-1 (Alliance mirror stub không bao giờ tạo):** `IAllianceWalletService.AddPointsAsync/DeductPointsAsync/RefundAsync` + optional `Guid customerId` → `AllianceWalletService.PublishLoyaltyChangedAsync` → `LoyaltyBalanceSyncPublisher` (payload customerId thật); call sites truyền: ledger (award/spend/refund/revert-order), OrderWorkflowService legacy, MissionService, RedemptionService (redeem + cancel), InternalLoyaltyController (3 DTO + pass-through), ShopERP `AllianceWalletServiceHttpProxy` (body forward); raw wallet calls giữ Guid.Empty (device fallback). **BUG-1b (mirror không giảm balance):** `LoyaltySyncSubscriber` bỏ MAX-merge → overwrite PG authority (T2.2 approved; MAX-merge = pre-cutover relic → spend không bao giờ giảm mirror → drift tăng vô hạn — RV bắt SQLite 150 vs PG 70). **RV production (E2E D2 qua deployed ledger, flip tạm 2 test tenant, cleanup pristine):** sync payload mang real customerId → shop-a SQLite: **stub customer Id = test customerId (identity trùng PG!)** + LoyaltyRewards row + history EARN 100/EARN 50/REDEEM −50 (BalanceAfter 70) + **"synced balance → 70"** — cả 2 bug verify production. **Finding nhỏ (defer):** double-delivery có thể xử lý events lệch thứ tự (1 lần quan sát) → mirror tạm stale giữa events (display-only, tự hội tụ; spend thật qua PG); fix đúng cần monotonic sequence + migration. Tests: AW-ATT-7/8 mới (payload customerId; raw → Guid.Empty) · LPI-B1-7 viết lại (SPEND 400→300 hội tụ) · Moq 7-arg (CS0854) + assert customerId thật (LED-5/6, LA-RD-1, LA-OW-1) → **Core.Tests 1745 PASS · ShopERP.Tests 99 · guard ALL PASSED · build 0 errors**. Push: FAST PUSH ×2 → CD Multi-VPS SUCCESS ×2 (CI + Accounting Tests cũng success). Cleanup: PG 0 wallets/0 tx/0 config, shop-a SQLite 0 stub (verified). Scripts RV đã xoá (rule 7).
* **2026-09-21 — LOYALTY POINTS INTEGRITY BATCH 5 — DEPLOYED + RV PRODUCTION PASS (`1b810476`, CD Multi-VPS SUCCESS).** Phase 5 — Alliance attribution khi tiêu điểm (D2 APPROVED). **Push:** `c1db4f6a` + `1b810476` (FAST PUSH — guard + build + Core.Tests đã chạy session) → CD SUCCESS ×1 (CI + Accounting Tests cũng success). **RV production (script `.devin/rv_b5_attribution.sh`, đã xoá sau RV):** L2 markers Gateway.dll (`api/platform/loyalty`=1 · `settlement`=2 · `WalletTenantBalance`=1) + VanAn.Shared.dll (`GetTenantBalancesAsync`=1) + VanAn.CoreHub.dll (`ComputeNetEarnByTenantAsync`=2) ✓ · L1 settlement: empty tenantId → 400 · no-data → zero report · **V7 E2E D2 đầy đủ qua deployed ledger + internal API** — flip tạm tenant "Vạn An Test" (`0dfab177`) + "Test HKD" (`6aaf19e4`) → Alliance member (config rows chưa từng tồn tại) → `POST /api/internal/loyalty/award` A=100 + B=50 → `spend` 80 tại B → **`newBalance:70`**, PG: **đúng 2 REDEEM entries** — `(spend_at=B, source=B, −50)` + `(spend_at=B, source=A, −30)`; Gateway log: `AllianceWallet DeductPoints … -80 → balance=70 (sources: 6aaf19e4…:50, 0dfab177…:30)` = ưu tiên tenant hiện tại + FIFO xác nhận trên production · settlement A: earned=100/consumed=30/chi-hộ=30/redeemed=0/outstanding=70 · settlement B: 50/50/0/80/0 — đúng plan · spend 60 tại A → `A→A −60` (ưu tiên điểm A), balance 10 · **idempotent replay** spend 80 tại B cùng key → cached, vẫn 2 REDEEM rows · **cleanup xong**: PG 0 wallets / 0 tx / 0 LoyaltyTenantConfigs / 0 test customer (verified); shop-a SQLite — không có row nào được tạo (xem BUG-1) · **L3 Playwright regression**: `rv-batch4.spec.ts` 2/2 (checkout estimate "~4545 điểm" + tracking "30 điểm") + `rv-batch3.spec.ts` 1/1 (loyalty-config admin 0 crash) — không regression. **🐛 BUG-1 (RV finding, PRE-EXISTING — Batch 1/2 hardening, KHÔNG phải Batch 5, CHƯA FIX theo yêu cầu "liệt kê không fix case by case"):** Alliance wallet publish sync event với `customerId: Guid.Empty` (wallet device-based — `AllianceWalletService.PublishLoyaltyChangedAsync`) nhưng `LoyaltySyncSubscriber.SyncRowCoreAsync` step 3 chỉ tạo mirror stub khi `customerId != Guid.Empty` → **khách CHƯA có row local: điểm Alliance award/spend không bao giờ xuất hiện trong SQLite mirror** (POS/offline đọc 0). Bằng chứng RV: E2E Alliance award/spend thành công trên PG nhưng shop-a SQLite 0 row mới. Latent vì production mode=Silo (0 Alliance tenant, 0 wallet) — nhưng sẽ bite khi tenant chuyển Alliance. Fix đề xuất (chờ duyệt): (a) ledger truyền real customerId xuống `AddPointsAsync`/`DeductPointsAsync` → publisher, hoặc (b) subscriber tạo device-based stub khi customerId empty + tenantId/device có. **Ghi chú:** 1 FAIL trong script RV (`Gateway.dll 'api/platform/loyalty/settlement'`=0) là lỗi PATTERN grep của script (route nằm ở 2 attribute class+action riêng) — không phải bug code; grep tách `api/platform/loyalty`+`settlement` đều PASS.
* **2026-09-21 — LOYALTY POINTS INTEGRITY BATCH 5 — CODE DONE (`c1db4f6a`).** Phase 5 — Alliance attribution khi tiêu điểm (D2 APPROVED). **T5.1** `AllianceWalletService.DeductPointsAsync` attribution-aware: helper `ComputeNetEarnByTenantAsync` = Σ EARN/ADJUST (by TransactionTenantId) − Σ |REDEEM| (by **SourceTenantId**; legacy REDEEM null → TransactionTenantId, giữ invariant Σ netEarn == wallet balance); consume **ưu tiên tenant hiện tại** (redeeming) trước, thiếu mới **FIFO** (earliest positive EARN/ADJUST) các tenant khác; **1 REDEEM entry per source** (`TransactionTenantId` = tenant tiêu · `SourceTenantId` = tenant sở hữu · `SetSourceTenant` set-once · `RefundTenantId` = tenant tiêu Q4); không đủ nguồn → reject atomic (không trừ một phần). **`GetTenantBalancesAsync`** (mới, `IAllianceWalletService` + `WalletTenantBalance` record) → breakdown attribution-correct; Gateway `GET /api/loyalty/wallet` breakdown chuyển sang dùng + clamp âm → 0 (trước: group theo TransactionTenantId → cross-tenant redeem trừ nhầm tenant tiêu — ví dụ plan: earn A=100/B=50 → redeem 80 tại B = REDEEM −50 (B→B) + −30 (A→B), breakdown A=70/B=0). ShopERP `AllianceWalletServiceHttpProxy.GetTenantBalancesAsync` stub (Gateway-only). **T5.2** `GET /api/platform/loyalty/settlement?tenantId=` (SystemAdmin, `LoyaltyConfigController`): `pointsEarnedAtTenant` (Σ EARN) · `pointsConsumedAtTenant` (Σ |REDEEM| SourceTenantId=tenant — gồm cả "chi hộ") · `pointsConsumedAtOtherTenants` (subset TransactionTenantId≠tenant) · `pointsRedeemedByCustomersAtTenant` (Σ |REDEEM| TransactionTenantId=tenant) · `outstandingPoints` = earned − consumed. Report-only (SystemAdmin quyết định bù trừ thủ công). Tests: **`AllianceWalletAttributionTests` mới (6)** — AW-ATT-1 (D2: redeem 80 tại B → 2 REDEEM entries + breakdown A=70/B=0 + Σ=balance) · AW-ATT-2 (redeem tại A 60 → A trước, breakdown A=10/B=0) · AW-ATT-3 (redeem > tổng → reject, 0 entry) · AW-ATT-4 (idempotency multi-source, không double-deduct) · AW-ATT-5 (ưu tiên current tenant dù earned trước) · AW-ATT-6 (legacy REDEEM null SourceTenantId → fallback TransactionTenantId trong breakdown; redeem mới consume từ tenant có netEarn dương) — **`LoyaltyConfigControllerTests` +3** (LA-LC-16 cross-tenant settlement A/B đúng · LA-LC-17 empty tenantId 400 · LA-LC-18 no transactions → zero report) — **`LoyaltyWalletControllerTests` +1** (LA-W-7 clamp âm → 0) + update LA-W-1/LA-W-6 sang `GetTenantBalancesAsync` → **Core.Tests 1743 PASS (+10) · ShopERP.Tests 99 · guard ALL PASSED · build 0 errors** (commit pre-commit guard PASSED). Chờ: push → CD Multi-VPS → RV (V7: earn A+B → redeem tại B → breakdown + settlement đúng; L1 API settlement; L2 markers Gateway.dll `GetTenantBalancesAsync` + `settlement`).
* **2026-09-21 — LOYALTY POINTS INTEGRITY BATCH 4 — DEPLOYED + RV PRODUCTION PASS (`5e9ada23`, CD Multi-VPS SUCCESS — sau cleanup disk vanan-shop-a).** Phase 4 — 1 công thức duy nhất (fix RC2). **T4.1** `3_CoreHub/Services/LoyaltyPointsCalculator.cs` (mới, pure static): `NetRevenue(subTotal, discountAmount)` clamp ≥ 0 (D1 — bỏ VAT + phí ship) + `Calculate(netRevenue, PointsFormula{rate,min,max,mode,vndPerPoint})` — Silo base×rate (rate 0 → fallback 0.1) / Alliance base÷VndPerPoint (0 → fallback 1000), clamp min/max 1 nơi, netRevenue ≤ 0 → 0 (không min-clamp đơn free); **`OrderWorkflowService.ProcessLoyaltyPointsAsync`** chuyển sang calculator (base `order.SubTotal − order.DiscountAmount`, mode-aware qua `ResolveFormulaModeAsync` khớp ledger routing). **T4.2** `PublicOrdersController.GetPublicOrder`: banner = `ILoyaltyPointLedgerService.GetAwardedPointsAsync(orderId, tenantId)` (D4 — số THẬT từ LoyaltyIssuanceRecord PG, null khi chưa award/pending/skip; `LoyaltyEnabled` giữ từ tenant settings fail-open); xoá `ComputePointsAwardedAsync` recompute + bỏ dep `IOptions<LoyaltyPointsConfig>` khỏi controller. **T4.3** `LoyaltyController.GetPointsEstimate` (`GET /api/loyalty/estimate?tenantId&subTotal&discountAmount`) — resolution giống award (appsettings → PG LoyaltyGlobalConfig → tenant settings), mode-aware, trả `{loyaltyEnabled, points, mode, netRevenue}`; `!loyaltyEnabled || !awardOnAll` → points 0 (không over-promise); KhachLink `Checkout.razor` bỏ formula client → gọi endpoint với `cartState.NetSubTotal` (pre-VAT) + discount 0 (cart không có discount), `LoyaltyEstimateDto` thay `TenantSettingsDto`. Tests: `LoyaltyPointsCalculatorTests` (11: D1 base 90k → 9.000 · min/max clamp · Alliance 90/180 · rate 0 fallback · VndPerPoint 0 fallback · netRevenue 0 → 0) + `PublicOrdersControllerTests` (5: completed/delivered → 250 thật · không award → null · pending → null + ledger không query · tenant disable → LoyaltyEnabled false) + `LoyaltyWalletControllerTests` cập nhật constructor (3 dep mới) → **Core.Tests 1733 PASS (+16) · ShopERP.Tests 99/99 · guard ALL PASSED · build sln 0 errors**. Commit `5e9ada23` FAST PUSH (CI đã validate trong session). **CD:** run đầu fail 2 lần tại "Copy ShopERP deploy files" — root cause **vanan-shop-a `/dev/sda1` 100% full** (containerd snapshots 3.0G + content blobs 1.2G + journal 207M; Gateway + KhachLink deploy OK) → user duyệt cleanup: `sudo docker image prune -a -f` (+3.6GB) + `sudo journalctl --vacuum-size=100M` (+107M) → disk 66% (3.2G free) → rerun CD **SUCCESS** (3 VPS + smoke test). **RV production:** L1 — `GET /api/loyalty/estimate?tenantId=0dfab177…&subTotal=100000&discountAmount=10000` → `{points:9000, netRevenue:90000}` (D1: 90k×10%; tenant không có per-tenant config → appsettings rate, min 10, max null) · discount 150k > subtotal → `{points:0, netRevenue:0}` · `GET /api/public/orders/01a0becd-…` → `{pointsAwarded:30}` = số THẬT ledger (recompute cũ = TotalAmount 55.000×10% = 5.500) · pending order → `pointsAwarded:null` · L2 — `VanAn.Gateway.dll` chứa `LoyaltyPointsCalculator` (1 hit) + KhachLink WASM deployed chứa UTF-16 `loyalty/estimate` + `estimate?tenantId` + `discountAmount=0` + type `LoyaltyEstimateDto` + `NetSubTotal` · L3/L4 Playwright mới `6_Testing/e2e-tests/rv-batch4.spec.ts` + `playwright-rv-batch4.config.ts` — **2/2 PASS**: (1) store `a5b6c7d8-…` → add "Com Trua RV" 50.000đ (nút "Đặt hàng" — showcase render variant; spec hỗ trợ cả "Thêm vào giỏ") → /checkout → banner `data-testid="loyalty-estimate-banner"` hiện **"Đơn hàng sẽ tích ~4545 điểm"** (50.000/1.1 = 45.454,55 net × 10% = 4.545 — client cũ ra 5.000 → proof server D1) · (2) `/order-tracking/01a0becd-…` → banner `data-testid="loyalty-points-banner"` **"Bạn đã nhận được 30 điểm thưởng!"** — 0 crash errors, screenshots `rv-b4-*.png`. Cleanup: cart test chỉ trong browser Playwright (localStorage) — không dữ liệu server; không để lại config/data test. Chi tiết: `docs/plans/loyalty-integrity-detail-coding-plan.md` §Phase 4.
* **2026-09-21 — LOYALTY POINTS INTEGRITY BATCH 3 — DEPLOYED + RV PRODUCTION PASS (`2c12852b`, CD Multi-VPS SUCCESS).** Budget caps đăng ký + enforce (Phase 3). **T3.1** Gateway `LoyaltyConfigController`: `PUT /api/platform/loyalty/tenant/{id}/config` nhận `monthlyPointsBudget`/`dailyPointsBudget`/`perCustomerDailyLimit`/`perOrderRateCap` (null = unlimited, rateCap fraction 0..1) + validation (âm → 400, rateCap > 1 → 400) → `SetBudgetCaps`; `TenantConfigDto` mở rộng caps + counters `PointsIssuedThisMonth/Today`; **mới `POST /tenant/{id}/reset-counters`** (scope daily|monthly → `ResetDailyCounter`/`ResetMonthlyCounter`, SystemAdmin). **T3.2** ShopERP `LoyaltyConfigApiClient` (DTOs + `ResetTenantCountersAsync`) + `LoyaltyConfigAdmin.razor` (form 4 budget field — trần đơn nhập % → /100 — + hiển thị counters + 2 nút Reset với modal xác nhận; fix Razor nested-quote bằng 2 method riêng). **T3.3** `ValcnV2_LoyaltyBudget` **default true** (flag = emergency OFF SystemAdmin, description cập nhật); `OrderWorkflowService` legacy path bỏ `if (budgetCheckEnabled)` — budget check + `RecordIssuanceAsync` chạy vô điều kiện khi budget service có mặt (khớp `LoyaltyPointLedgerService.AwardAsync` đã unconditional từ Batch 2; ledger KHÔNG đọc flag — D3). **T3.4** verify (không đổi): `RefundOrchestrationService` Step 2c → ledger `RevertOrderAsync` (mode-aware, gate-safe) + `DecrementIssuanceAsync` theo total reversed. Tests: **`LoyaltyBudgetServiceTests` mới (11)** — monthly 1000→600+600→400, exhausted→0, daily cap, rate cap 3%×100k=3000 + null orderAmount skip, per-customer daily qua PG issuance record (A=50/B=100, reversed không count), RecordIssuance atomic, Decrement reversal giải phóng budget + clamp 0, no-config = original — **`LoyaltyConfigControllerTests` +7** (LA-LC-15..21: persist + update giữ counters, âm 400, rateCap 1.5 400, reset daily/monthly, scope invalid 400) → **Core.Tests 1717 PASS · build 0 errors · guard ALL PASSED**. **RV production:** L1 API (PUT persist + GET lại OK; monthly=-5 400; rateCap=1.5 400; reset daily/monthly 200 + counters 0; scope=yearly 400 — lưu ý API trả **camelCase**, check script phải đúng case) · L2 Gateway.dll markers (`MonthlyPointsBudget`=1, `reset-counters`=1) · L3/L4 Playwright app2: sysadmin UI login → `/admin/loyalty-config` → chọn tenant "Vạn An Test" → budget form (4 field) + counters + reset buttons render, **0 page errors**, input rỗng = unlimited → **PASS** (`6_Testing/e2e-tests/rv-batch3.spec.ts` + `playwright-rv-batch3.config.ts`). **Cleanup:** config row "Vạn An Test" xóa (không tồn tại trước RV) — production không để lại dữ liệu test. L5 manual pending.
* **2026-09-20 — LOYALTY POINTS INTEGRITY BATCH 2 — DEPLOYED + RV PRODUCTION PASS (`962fa197` + `6b9a667e` + `5f7f747d`, CD Multi-VPS SUCCESS ×3).** Phase 1 + backfill T2.3: PG ledger = single source of truth. T1.1 `AllianceTransaction.SourceTenantId` + `SetSourceTenant`. T1.2 `ILoyaltyPointLedgerService`/`LoyaltyPointLedgerService` (Award/Spend/Refund/RevertOrder/GetAwardedPoints/GetBalance — budget luôn enforce D3 · guard orderId tập trung RC3 · tenant attribution RC2.4 · luật Silo · issuance record PG · flag `LoyaltyLedgerV2` default ON rollback). T1.3 `InternalLoyaltyController` +5 endpoint. T1.4 ShopERP POS proxy cutover (LoyaltyRewardsServiceHttpProxy + LoyaltyPointLedgerServiceHttpProxy, D5 graceful). T1.5 AllianceWallet defense-in-depth budget + `ILoyaltyBudgetService` orderAmount nullable. T1.6 OrderWorkflowService → ledger (legacy fallback). RefundOrchestrationService Step 2c → RevertOrderAsync. **Domain: LoyaltyRewards 1:1→1:N** (1 row/tenant — EF 1:1 từng DELETE row cũ khi tạo row tenant 2 — chính là RC2.4). Migrations: PG `20260920124823` + `20260920131530`, ShopERP `20260920131734`. Design-time factory fix Npgsql legacy timestamp (snapshot drift). Tests +19 → **Core.Tests 1699 PASS · build 0 errors · guard ALL PASSED.** Chờ: push → CD → backfill `loyalty-backfill-pg` → RV V2/V3/V8.
* **2026-09-20 — LOYALTY POINTS INTEGRITY BATCH 1 — DEPLOYED + RV PRODUCTION PASS (`ebbc1f5a` + `6023ad0`, CD Multi-VPS SUCCESS ×2).** User bug: "điểm thưởng được tính nhưng không chính xác, cũng không cộng vào khách". Review (REVIEW_ONLY) tìm RC1 (Silo award trên Gateway ghi **PG**, khách đọc **ShopERP SQLite** — event cũ `loyalty.points.changed` chỉ PushNotification consume, không sync DB; subject outbox `vanan.cloud.loyalty.points.changed` không khớp subscriber `vanan.cloud.loyalty.changed.>`) + RC2 (3 lệch công thức: checkout estimate không clamp, banner recompute estimate, ShopERP ignore `LoyaltyGlobalConfig` trong EF model → fallback appsettings) + RC3 (guard double-award per-DB) + 3 yêu cầu sâu (tenant attribution Silo mất — repo lookup bỏ tenant dimension; budget `SetBudgetCaps` không có API + flag `ValcnV2_LoyaltyBudget` OFF; luật mode Silo/Alliance). **Plan 5 phases đã duyệt** (`docs/plans/loyalty-integrity-detail-coding-plan.md`, D1 net revenue / D2 ưu tiên tenant hiện tại / D3 luôn enforce / D4 banner thực tế). **Batch 1:** `LoyaltyBalanceSyncPublisher` (subject `vanan.cloud.loyalty.changed.{deviceId}` + payload mở rộng + Outbox `LoyaltyChanged` RoutingKey=deviceId) · `LoyaltyRewardsService` Silo publish sync sau EARN/SPEND (giữ legacy push) · `AllianceWalletService` delegate publisher · `LoyaltySyncSubscriber` match (tenantId,customerId)→device fallback → **tạo customer stub + row khi chưa có local** (guest Gateway-completed — lỗ hổng phát hiện giữa RV), **max-merge balance** (bảo toàn điểm POS khi PG chưa là authority), history idempotent · backfill `POST /api/admin/sync/loyalty-backfill-pg` (SQLite→PG additive, dedup timestamp+points+reason, row history rỗng skip). **RV production:** đơn thật `01a0becd` (tenant "Vạn An Test" loyalty ON — tenant 1 hóa ra `Loyalty_Program_Enabled=false`, config drift PG vs SQLite ghi nhận), guest checkout → **complete qua Gateway** `PUT api2/api/orders/{id}/status` (Bearer impersonate) → 🎁 Awarded 30 (rate 0.1, clamp max 30 từ LoyaltyGlobalConfig PG) + `LoyaltyIssuanceRecord` + guard "Skipped duplicate" (delivered→award, completed→skip) → shop-a prod log: `created customer stub eb5b6a96… for loyalty mirror` → `created LoyaltyRewards row` → `appended history (EARN,30)` → **`synced balance → 30`**. Core.Tests **1680 PASS** (+9: publisher 4 + subscriber 5) · build 0 errors · guard ALL PASSED · FAST PUSH ×2 (--no-verify, CI validated in session). API host đổi: checkout = `api2.khachvip.online` (api.khachvip.online hết resolve). Test data: order `01a0becd` + customer `eb5b6a96` (tenant "Vạn An Test") giữ nguyên — tenant test. Batch 2 (PG ledger authority + POS proxy) chuẩn bị session mới.

* **2026-09-19 — ISSUE BATCH #176-#181 BATCH 2 — DEPLOYED + RV PRODUCTION PASS (`a620d1de`, CD Multi-VPS SUCCESS).** Push `a620d1de` (FAST PUSH — guard + build + Core.Tests 1669 đã chạy trong session) → CD SUCCESS. **RV spec mới `6_Testing/e2e-tests/rv-batch2.spec.ts` + `playwright-rv-batch2.config.ts`** (Playwright production, sau khi build+deploy xong). **#176: 0 crash errors trên diemthuong2 store page → ISSUE CLOSED.** **#181:** add product thật (store page "Đặt hàng") → drawer home=true · /cart=0 · /checkout=0 (navCart 0/0) — hết 2 giỏ. **#178:** salesman/products API 401 đúng + page load OK (còn luồng salesman login — chưa có tài khoản). **#179 wizard E2E:** sysadmin UI login → /quick-setup?tenantId=<test tenant> → Quán Cafe → shop info → Bắt đầu khởi tạo → **success hiện "✅ 32 sản phẩm · ✅ 15 nguyên liệu" = số THẬT từ `GetSeedCountsAsync`**; SQLite verify (db+wal): tenant F60B64D0 có 32 Products/15 Ingredients/recipes (CreatedAt 17:06 đúng lúc wizard). **Bài học: TenantId lưu UPPERCASE trong SQLite (TenantIdConverter) — query thủ công phải đúng case, SQLite `=` case-sensitive (lần đầu query lowercase ra 0 → tưởng seed fail)**. Điểm sáng: seed mechanism không cần Tenants row (không FK); tenant-sync gap pre-existing (quick-setup tenant không có SQLite Tenants row — ghi nhận, không chặn seed). Test tenant `f60b64d0` + users/groups/crawl đã cleanup (PG DELETE, TaxCode LIKE 'RVB2%').
* **2026-09-19 — ISSUE BATCH #176-#181 BATCH 2 — CODE DONE (`6ab09cbc`).** Build 0 errors · Core.Tests **1669 PASS** (+10) · guard ALL PASSED. **#181** header cart icon + floating CartDrawer ẩn tại `/cart`/`/checkout`/`/scan` — `HideCartChrome` (Navigation.Uri route check) + `LocationChanged` → StateHasChanged (kể cả query-only nav như `/scan?code=...`); dispose unsubscribe. **#176** sweep `disabled=` lowercase → `Disabled` PascalCase 5 chỗ (VanAOrderTable ×2 / VanAStatusForm / VanAStaffForm / Checkout.razor) — `grep 'VanAnButton[^>]*disabled='` = 0. **#178 phần 2 — "Gian hàng của tôi":** `ISalesmanService.GetSalesmanStoreAsync/AddProductToStoreAsync/RemoveProductFromStoreAsync` + refactor `LoadSalesmanRoleWithCodeAsync` (backfill code, tách từ GetCompositeSalesmanQrAsync — 28/28 tests giữ nguyên); Gateway `GET /api/community/salesman/products` (products đã config: composite QR + giá live từ FeaturedProducts + shop name + availableForAdd) · `POST .../add` (ProductReferralConfig defaults 0.01/1000 — an toàn, không cho salesman tự set rate) · `POST .../remove` (DeactivateAsync soft); KhachLink `/community/salesman-store` page (QR canvas per product + Tải QR + Đồng bộ giá (composite code không đổi → QR cũ vẫn hoạt động) + Xóa confirm + Thêm từ availableForAdd) + nav desktop + mobile tab; 6 test mới. **#179 — RV production (vanan-shop-a):** SQLite `/app/keys/vanan_shoperp.db` — tenant `1833b55c-1022-4a6c-8056-8935f503a62b`: **0 Products + 0 Tenants row**; container log 52.504 dòng (start 13:33Z) KHÔNG có `ApplyTemplateAsync`/`seed complete`/`Error during setup` → seed chưa persist trên container này (log lần chạy user bị mất do redeploy hôm nay); seed mechanism vẫn HOẠT ĐỘNG — 2 tenant (35037648, B391C72B) có đủ 32 products F&B nhưng cũng KHÔNG có Tenants row (Products không FK Tenants — seed không cần tenant row). Defect verified: exception bị nuốt (`QuickSetup.razor catch { Console.WriteLine }`) + success screen hardcoded (`selectedTemplate.Products` 32/15/4). **Fix:** `IOnboardingService.GetSeedCountsAsync(shopId)` (đếm Products/Ingredients/Recipes thật từ DB) + `ProcessSetupAsync` đọc counts thật sau seed + error state đỏ step 4 (không bao giờ fake success) + `QuickSetupResponse.Error`; 4 test mới (`OnboardingTemplateServiceTests`: seed 32/15/recipes>0, idempotent skip, unknown template throw, empty counts 0). Task cards cập nhật status. Chờ: push → CD → RV (A3 quick-setup tenant test, #176 store page, #181/#178 UI).

* **2026-09-19 — ISSUE BATCH #176-#181 BATCH 1 — VERIFY + FIX + DEPLOY + RV (`49d07bd5` + `7a32a9c7`).** Verify 6 issues mới (comments đã post cả 6). **#180** Verify Pending Tenant 400 — root cause 2 lớp: Slugify giữ dấu TV → `UpdateSlug` regex reject; fix v1 NFD+`\p{Mn}` PASS dev nhưng vẫn fail production vì **`VanAn.Gateway.runtimeconfig.json` chạy `System.Globalization.Invariant=true` → Normalize(FormD)/\p{Mn} là NO-OP trên runtime deployed** (bằng chứng: probe reflection chạy deployed DLL trên Windows cho slug hợp lệ, cùng DLL trong container gateway → 400; `/proc/1/maps` cùng inode; restart không đổi). Fix v2 = **explicit Vietnamese diacritics map** (á→a…đ→d) + controller catch `ArgumentException` → BadRequest(ex.Message) + client `SendAndReadAsync` surface body lỗi. **RV: tenant "Quán Cà Phê Đậm Vị" verify không slug → HTTP 200, publishedSlug `quan-ca-phe-dam-vi-1789825049`.** **#178** Domain `ProductReferralConfig.CommissionRate` 0.02-0.05 → **0-0.5** (duyệt), UI default 0.01/1000, tests T18/T18b — **RV: POST rate=0 → 201.** **#177** client-only (duyệt): `ReferralScanResult`+`ProductType`, `Scan.razor` truyền IsFree (referral + QR legacy path enrich catalog fallback Free), deserializer + JsonStringEnumConverter. **#176** đã fix sẵn (`c76c0b4d` trong HEAD) — chờ RV đóng. **#179** cần RV app2 (wizard dùng số hardcoded + exception nuốt). Core.Tests 1659 PASS · guard ALL PASSED · build 0 errors · CD Multi-VPS SUCCESS ×2. **RV cleanup: 7 tenant test xóa sạch + restore config referral `38df680d` (COMRV1) bị RV script deactivate nhầm.** Master plan + 6 task cards: `docs/AI/tasks/issues_176_181/`. Batch 2 (#181/#178-phần2/#179/#176) chuẩn bị session mới.

* **2026-09-19 — NAV MENU SSOT + SPA NAVIGATION (`4643c69e`).** Fix menu sidebar không ổn định (user feedback): 4 layout tự build menu riêng → cùng role thấy menu khác nhau khi duyệt trang; `<a href>` thuần → full reload mỗi lần bấm menu. **Root cause (review):** R1 — 4 layouts (MainLayout/AdminLayout/AccountingLayout/EInvoiceLayout) × 4 menu riêng · R2 — `VanANavigation` dùng `<a href>` không phải `<NavLink>` → Blazor Server full reload · R3 — NavMenu vs AdminLayout lệch dữ liệu cho cùng role (pattern từng gây bug `rv_report_wave3.md` FAIL-1). **Fix:** `ShopErpMenuService` (DI) = SSOT menu theo role, 4 layout gọi chung; `VanANavigation` → NavLink (SPA, không reload) + auto-expand group theo URL + giữ trạng thái mở/đóng khi điều hướng; bổ sung items thiếu (Owner + Tài chính; SystemAdmin + Claims/Crawl/HĐĐT/Tài chính/Hướng dẫn); giữ conditional HKD/Enterprise + Issue #103 impersonation. Validation: build sln 0 errors · Core.Tests 1648 PASS · ShopERP.Tests 99/99 · Architecture 41/41 · guard ALL PASSED · CI + Accounting Tests + CD Multi-VPS SUCCESS. **RV production 23/23 PASS** (`6_Testing/rv-nav-menu.js` + screenshots): Owner + SystemAdmin — menu đủ nhóm, bấm menu không full reload (window marker + performance nav entries 1→1), menu nhất quán qua 5 trang cross-layout; DLL markers VPS: `ShopErpMenuService`=2 · `ToggleGroup`=1 · `IsGroupOpen`=1. L5 manual pending.

* **2026-09-18 — REALTIME PLATFORM MANUAL-TEST ROUND (`0e50cf51` + `c76c0b4d`).** 3 findings user: (1) **map xám** — OSM tiles bị chặn (verify OSM 000 / CARTO+Esri 200) → **tile fallback chain** trong realtime.js (OSM→CARTO→Esri, switch trên tileerror, burst-lock) — verify production tiles 15/15 từ CARTO · (2) chat auto-load **8s→4s** + **nút làm mới** header · (3) mobile nút Gửi tràn → **stack dọc <576px** (RealtimeChatPanel.razor.css ::deep). **2 bug nữa từ debug dump:** VanAnButton render crash khi truyền `disabled` lowercase (va chạm `@attributes` + disabled nội bộ) → dùng param `Disabled`; `@onkeypress` qua unmatched attributes render literal → thêm param `OnKeyPress` cho VanAInput (Enter-to-send). **Verify:** debug-map + debug-panel-html + 16/16 E2E PASS trên production · CI ✅ (kèm fix teardown flaky Integration `3c009f7d` — `StaffRoleWebApplicationFactory.Dispose` host trước connection sau) · CD Multi-VPS ✅.

* **2026-09-18 — REALTIME PLATFORM RV P1-P5 TRÊN PRODUCTION (`b489ffb6`).** Script `.devin/rv-realtime-p1-p5.sh` dùng order thật từ PG (SSH vanan-gateway → psql VanAnCoreHub): `01a0afac` (customer + device `81f43d82`) + `01a0b4a8` (guest + device `c9ea713e`). **L1 API 26/26:** legacy chat (guest 200 / wrong 403 / no-id 401 — device trên customer-order = 403 legacy by design, generic mới resolve qua Order.CustomerDeviceId) · tracking buyer/guest 200 + shopLat · generic Order history/send/round-trip/latest · Shop 401/create/send/privacy · hubs ×4 · L2 3 host. **L3 Playwright 13/13:** + `rv-realtime-order-ui.spec.ts` — order thật: chat panel ENABLED (fix P6), guest gửi tin qua UI xuất hiện, map leaflet render, 0 console error. **Bug thứ 3 RV bắt (`b489ffb6`):** device send trên order → 403 (conversation keyed theo CustomerId, device không phải participant) → fix `EnsureParticipantAsync` (Guest) cho Device trên Order subjects + T12. 69/69 Realtime tests · CI ✅ · CD Multi-VPS ✅. L4/L5 manual pending.

* **2026-09-18 — REALTIME PLATFORM P6 — E2E + RV L1-L3 + REUSE GUIDE (`3fb71866`).** E2E specs mới chạy thẳng production: `realtime-shop-chat.spec.ts` (7) + `realtime-tracking.spec.ts` (5) = **12/12 PASS** qua `realtime-rv.config.ts` (config bỏ global-setup local + storageState, giữ reporters/timeouts). **RV:** L1 API (401/guest-create/send-readback/privacy-filter/400-validation) ✅ · L2 `_content/VanAn.UI.Platform/js/realtime.js` + leaflet **200 trên 3 host** (diemthuong2, api2/shoperp, timlathay) ✅ · L3 Playwright UI (store chat guest send + order-tracking render, 0 console error) ✅ · L4/L5 manual pending. **Bug 1 — Shop chat chicken-and-egg** (deploy `7406d83a` → fix `f05960c5`): khách mới 403 mãi vì authorizer trước ensure. Fix = **public widget**: Shop ensure create-only trước authorize; privacy = **history filter per-caller** (tin mình + shop reply; staff toàn bộ) + **per-user SignalR group** `msg_Shop_{tenant}_u_{userId}` (staff shared group, khách group riêng, push cả 2). Tests T10/T11 (filter/staff-all) + E2E P5-5 (device khác không thấy tin). **Bug 2 — Razor string-param binding** (deploy `3fb71866`): `Param="_field"` không `@` = **literal string** khi param nhận string → `CustomerToken="_customerToken"` gửi literal → 401 → chat input disabled; **ChatPanel + LeafletMap shim (P4) cũng dính — order chat logged-in 401 từ lúc P4 deploy** (E2E mới lộ). Fix: bind `@` cho mọi string param không-literal; verify literal `_customerToken` = 0 trong WASM. **Reuse guide:** `docs/UI_Platform_Implementation_Guide.md` + §Realtime (quick start 5 bước, checklist, identity, endpoints, static assets, E2E). Build 0 errors · guard ALL PASSED · 68/68 Realtime tests · Core.Tests 1647 PASS · CI ✅ · CD Multi-VPS ✅. Chi tiết: task card Section 18.10.

* **2026-09-18 — REALTIME PLATFORM P5 — SHOP CHAT + INBOX (CODE COMPLETE, chưa commit/deploy).** Consumer đầu tiên của platform. **Server:** `ShopRealtimeAuthorizer` (keyed Shop: staff `tenant_id` claim == SubjectId ∨ conversation initiator ∨ participant row; tenant-scoped) · `IRealtimeParticipantAuthorizer` + overload tenant (default impl giữ P2 semantics) · `RealtimeIdentity` + `TenantId` (StaffJwtValidator đọc `tenant_id` claim) · lookup + 2 hubs truyền tenant · `RealtimeMessagingService.EnsureParticipantAsync` public (staff thành participant trước send) + `GetConversationsAsync` (inbox) · `RealtimeController`: ensure conversation Shop (`EnsureConversationAsync(tenant, Shop, subjectId, userId, subjectId, role, Shop)`) + **`GET /api/realtime/shop/conversations`** (staff-only, last-message preview + customer name 1-pass PG). **KhachLink:** Store.razor tạo `customer_device_id` ngay (F8) + section chat `RealtimeChatPanel SubjectType="Shop"` (token hết hạn → fallback guest) · GoogleMaps.razor viết lại = `VanAnMap` tĩnh + khoảng cách haversine, bỏ `<style>` inline (F9/F11). **UI.Platform:** staffToken (Bearers HTTP + `?access_token=` SignalR) qua interfaces/adapter/panel; guest giờ cũng connect SignalR. **ShopERP:** `ShopInboxApiClient` (Owner JWT) + `/community/messages` (AdminLayout, [Authorize Owner], list + RealtimeChatPanel) + nav + App.razor RCL scripts. Build 0 errors · 66/66 Realtime tests (56 + T1-T9 + T12) · Core.Tests 1645 PASS. Deviations: authorizer overload tenant (bắt buộc — shop side là tenant), inbox endpoint mới, không feature-flag shop chat, unread count defer. Chi tiết: task card Section 18.9.

* **2026-09-18 — REALTIME PLATFORM P3 + P4 DEPLOYED (`d8072ec5`).** Fast push `7e732f04..d8072ec5` (`--no-verify` — guard + build + Core.Tests 1635 đã chạy trong session) → **CI ✅ · Accounting Tests ✅ · CD Multi-VPS ✅** (deploy 3 VPS + smoke test). **RV L2 (production, diemthuong2.khachvip.online):** `/_content/VanAn.UI.Platform/js/realtime.js` **200** (6019B, chứa `vananMap` + `vananRealtime`) · `/_content/VanAn.UI.Platform/lib/leaflet/leaflet.js` **200** (147557B) + `leaflet.css` **200** · index.html deployed trỏ cả 3 RCL assets và **không còn** `/js/leaflet.js` cũ → F4 "RCL static assets trên host" verified. P3+P4 now live: generic `/hubs/messaging` + `/hubs/tracking` + `/api/realtime/*`, KhachLink chat/map trên generic surface (shims giữ route cũ). RV L3-L5 (Playwright + UI flow + manual) thuộc P6.

* **2026-09-18 — REALTIME PLATFORM P4 — UI PLATFORM EXTRACTION + KHACHLINK MIGRATION (CODE COMPLETE, chưa commit/deploy).** UI-1..UI-13 + F4 + F11 từ task card. **UI.Platform (mới):** `IRealtimeEndpointProvider` (F4 — derive Gateway URL từ app origin, port `DeriveGatewayUrl` C8; default `NavigationRealtimeEndpointProvider` hoạt động WASM/Server/SSR) · `IRealtimeChatClient`/`ILiveLocationClient`/`IMapJsAdapter` · `RealtimeHttpAdapter` (named client `realtime`, **absolute URLs per call** — hết vấn đề base-address scoping WASM-vs-Server; trackerId KHÔNG phải client param — server stamp identity) · `LeafletMapAdapter` · `RealtimeChatPanel`/`VanAnMap` (F11: VanAInput thay `<input class="form-control">`; 404 = empty state) · `wwwroot/js/realtime.js` (`vananRealtime` + `vananMap`) + vendored Leaflet · `AddRealtimePlatform()` đăng ký 3 host (KhachLink/ShopERP/Directory). **KhachLink:** ChatPanel/LeafletMap → shim · OrderTracking → `VanAnMap` + `ILiveLocationClient.GetLatestAsync` + join `/hubs/tracking` (legacy hub giữ cho status pushes — GW-6) · DeliveryTracking → `RecordPingAsync("Order", OrderId, ...)` · ChatHttpService xoá · index.html trỏ `_content/VanAn.UI.Platform/...` · LocationTrackingService → `vananRealtime.getCurrentPosition`. **Gateway:** `RealtimeController` + `IChatService` — fallback ensure khi `subjectType == Order` và conversation miss (đơn mới không 404 trên generic surface; caller đã qua authorizer). **F4 verify:** publish KhachLink có `_content/VanAn.UI.Platform/js/realtime.js` + `lib/leaflet/*`. Build 0 errors · guard ALL PASSED · 56/56 Realtime tests (37 P2/P3 + T24-T27 fallback + T1-T15 UI Platform) · Core.Tests 1635 PASS. **Deviations:** trackerId bỏ khỏi client interface (server stamp) · fallback Order→legacy ensure (bắt buộc cho migration) · ChatHttpService xoá hẳn · OrderTracking giữ legacy hub cho status (3 SignalR connections khi login — chấp nhận) · ping generic Order/orderId (DeliveryTaskId=Empty trên ping mới — legacy endpoint không thấy, UI đã migrate). Chi tiết: task card Section 18.8.

* **2026-09-17 — REALTIME PLATFORM P2 DEPLOYED + RV L1 PASS (`2c3e0360`).** Pushed `96e733a6..2c3e0360` (fast push `--no-verify` — guard + build + Core.Tests đã chạy trong session) → CI ✅ · Accounting Tests ✅ · CD Multi-VPS ✅. **RV L1 (Gateway PG):** `ConversationParticipants` tồn tại (PK + unique `(ConversationId, ParticipantId)` + index `ParticipantId`); `Conversations.SubjectType/SubjectId` + `DeliveryTrackings.SubjectType/SubjectId/TrackerId` đúng kiểu NOT NULL/nullable; unique index `IX_Conversations_TenantId_SubjectType_SubjectId` tạo đúng, `IX_Conversations_OrderId` đã thành non-unique; index `IX_DeliveryTrackings_TenantId_SubjectType_SubjectId_RecordedAt` có mặt; **backfill 7/7 conversations + 40/40 pings** → migration an toàn trên dữ liệu thật (đúng rủi ro F2 đã dự đoán). Note: full Core.Tests 1586 passed + 1 flaky timing test (`SQLiteConcurrencyIntegrationTests.BatchProcessing…`, pass 3/3 khi chạy riêng, chỉ chạm Orders — không liên quan P2).
* **2026-09-17 — REALTIME PLATFORM P2 + REVIEW P2-P6 (11 findings).** Review P2-P6 (REVIEW_ONLY, `review_plan_compliance`+`review_domain`+`review_ui`) tìm 11 findings. **F1 (BLOCKER):** P5 dựa trên tiền đề sai — Logistics/JobMarket chỉ là preset nav-flags (`KhachLinkNavFlags.cs:52-53` vẫn `TODO R3`; sprint8/9 ⏳ PENDING), không tồn tại entity `Shipment`/`JobApplication` → không chứng minh được SC6. **F2 (BLOCKER):** unique index `Conversation.OrderId` không tenant/subject-scoped → conversation generic thứ 2 toàn DB vi phạm. **F3:** guest không có SignalR (hubs chỉ nhận `customerToken`), D6 mới vá HTTP. **F4:** `UI.Platform` không có `wwwroot/` lẫn HTTP adapter; chưa định nghĩa cách RCL lấy `HttpClient`. **F5:** `ChatService` gắn chặt Order → SVC-7 "adapter mỏng" bất khả thi. **F6:** index tracking vô dụng với ping generic. **F7:** không có UI chat phía shop. **F8:** `/store/{slug}` anonymous + device id chỉ tạo ở Checkout. **F9:** map trang shop là Google iframe (không phải Leaflet). **F10:** state ghi `c94a490f` vs HEAD `d5e055a0` + 2 file diff thuần CRLF. **F11:** vi phạm UI Platform trong component sẽ port. **Quyết định user:** GPS trang shop = map tĩnh + khoảng cách · P5 = Shop chat (Logistics/JobMarket → P7) · phía shop = trang inbox `/community/messages` · thứ tự P2→P3→P4→P5. **P2 IMPLEMENTED:** `RealtimeSubjectType` (+`Shop`) · `Conversation.SubjectType/SubjectId` + generic ctor + `AssignCounterpart` · `ConversationParticipant` (NEW) · `DeliveryTracking.SubjectType/SubjectId/TrackerId` · `RealtimeMessagingService` + `LiveLocationService` + `IRealtimeParticipantAuthorizer`/`OrderRealtimeAuthorizer` (keyed DI) · migration `20260917101938_AddRealtimePlatformP2` + backfill `SubjectId` · fix F2/F6. `IChatService`/`ChatService` giữ nguyên. Build 0 errors · guard ALL PASSED · 11/11 test mới · 248/248 Community regression. Task card Section 18.6 + 20.
* **2026-09-17 — REALTIME PLATFORM P1b — GPS MAP + FREE/CHARITY CHECKOUT (`96e733a6`).** Manual test sau P1 phát hiện chat OK nhưng GPS/map chết + Free/Charity không tạo được order. **D7** shipper không thấy map vì `DeliveryTracking` lấy toạ độ từ `nearby-orders` — endpoint **loại trừ đơn đã có DeliveryTask active** (chính đơn đang xem) → fix dùng `/api/community/orders/{id}/tracking`. **D8** map khách trắng do toạ độ `0` vẫn qua `HasValue` (tâm 0,0) + marker chỉ add ở first render → fix chuẩn hoá `0→null` ở endpoint, client center shop→delivery→shipper, `LeafletMap` upsert marker mỗi render. **D9** Free/Charity không tạo được order vì ShopERP product API không có `productType` → cart `IsFree=false` → Tier 0 chặn giá 0 → fix 2 lớp: Gateway resolve Free/Charity từ `FeaturedProducts` trước price guard (authoritative, giữ spoof guard) + `Store.razor` enrich ProductType. Push `b12a99d2..96e733a6` (pre-push CI ALL PASSED) → CD Multi-VPS SUCCESS. **RV:** D9 API `IsFree=false`→200 / spoof→400 · Store add → `IsFree=true IsCharity=true` · customer map `.leaflet-container`+2 markers+8 tiles, 0 console error · tracking endpoint trả `shopName`+coords+ping cho đơn có task active · L2 served `upsertMarker`. L5 manual pending.
* **2026-09-17 — REALTIME PLATFORM P1 DEPLOYED + RV PASS (`b12a99d2`).** Pushed to `main` (CI pre-push ALL PASSED: build 429s · unit 1567 · startup · arch+integration 276) → CD Multi-VPS SUCCESS. RV: **L1** new endpoint 401/404 JSON (was SPA HTML), guest device match → 200 with shop coords + live shipper ping, wrong device → 403, guest chat history 200 + send 200 (persisted) · **L2** deployed WASM contains `X-Customer-Device-Id`/`_customerDeviceId`/`GetOrderTrackingAsync`/`LoadTrackingAsync`; ShopERP container recreated at CD time · **L3** Playwright guest `/order-tracking/01a0ad6a…` → chat panel + map + guest message visible, 0 console errors · **L4** guest typed + clicked Gửi in the UI → message appeared. **L5 manual pending.** RV artifacts deleted.
* **2026-09-17 — REALTIME PLATFORM P1 IMPLEMENTED.** Fixed 6 production defects (D1-D6): `SocialAuthController` now enqueues `CustomerCreated` for Google/Facebook customers (+ backfill on next login) so `Order.CustomerId` is no longer nulled → chat + GPS work for logged-in customers; new buyer-accessible `GET /api/community/orders/{orderId}/tracking` (OrderTracking no longer calls shipper-only `nearby-orders`; `_showMap` set on coords/live ping); checkout captures `DeliveryLat/Lng`; shipper GPS resumes on page load; guest chat/tracking via `X-Customer-Device-Id` (`ValidateCustomerOrDeviceAsync`, `ChatService.guestDeviceId`, `ChatPanel` HTTP-only + 8s polling). Files: `SocialAuthController.cs`, `CommunityController.cs`, `IChatService.cs`, `ChatService.cs`, `CommunityHttpService.cs`, `ChatHttpService.cs`, `ChatPanel.razor`, `OrderTracking.razor`, `DeliveryTracking.razor`, `Checkout.razor`. Build full sln 0 errors · 40/40 chat+delivery tests PASS. **P2-P6 pending.**

* **2026-09-17 — REALTIME PLATFORM OBJECTIVE + GUEST DOCS.** Set Section 2 = Realtime Platform (chat + live location reusable). Moved commission objective → Section 6 history. Added task card `docs/AI/tasks/realtime_platform/task_card_01_realtime_chat_gps_reusable.md` (approved: guest CÓ chat+tracking via `X-Customer-Device-Id`; Domain additive approved; consumers = Logistics + JobMarket). Fixed guest docs: `07-customer.md` §2.1/§2.4/§5.1/§6.1/FAQ (guest no longer "KHÔNG tracking + KHÔNG chat"), `04-shipper.md` §8.3, `README.md` §3.3. Refreshed branch refs `05443115` → `c94a490f`. **Implementation P0-P6 not started.**
* **2026-09-17 — DEV-TOKEN SECRET SAFETY RULE + STATE SYNC.** Added `.devin/rules/dev-token-secret.md` (hard-stop security rule: `DevToken__Secret` unset by default; never persisted in `.env.shoperp`/repo/commit; inline shell env only; never echoed; always removed + verified `404` at the end of an RV window; checklist + reporting requirements) and referenced it from the always-on `governance.md` ("SECRETS & TEST-AUTH (HARD STOP)") + `task_card_04_dev_otp_gate.md`. Also fixed two stale follow-ups in Section 4 that contradicted the completed work (whole-order commission + hardcoded QR host are both fixed) and refreshed the branch/HEAD references to `05443115`.
* **2026-09-17 — COMMISSION BASE = REFERRED PRODUCT + SELF-REFERRAL BLOCKING (`05443115`).** (1) Commission was computed on the whole order (`order.TotalAmount` = SubTotal + VAT + ShippingFee) and `WalletService` used a different formula (`margin × rate`) → balance invariant could disagree with the payout. New `ReferralCommissionCalculator` = single source of truth: base = referred line items' SubTotal (pre-VAT, no shipping); referred product absent → no commission; Reseller OnMargin → order margin pro-rated by the referred share. `SalesReferral.CommissionBaseAmount` (audit) + migration `20260917011937`; WalletService includes Items + same calculator. (2) Self-referral was never blocked (`SameFingerprint` hardcoded false, no salesman-vs-buyer check) → a salesman buying their own code got a Pending commission auto-paid after 24h. Layered fix: checkout drops the referral when it resolves to the buyer; `CreateCommissionAsync` detects via CustomerId / device fingerprint / device token (guest via `Order.CustomerDeviceId`); new `RiskScoreInput.SelfReferral` weight 100 → Rejected + `FraudFlag(SelfDeal)`. Build 0 errors · Guard ALL PASSED · 50/50 tests · CD Multi-VPS SUCCESS. RV: migration applied; order sub-total 250,000 with a 50,000 referred line → base 50000 / commission 1500 (not 8250); self-referral at checkout → no attribution; forced buyer==salesman → Rejected, RiskScore 100, `SelfReferral:+100`, FraudFlag SelfDeal; commissions API pending 3150 / rejected 1500. Task card: `task_card_07_commission_base_selfreferral.md`.
* **2026-09-17 — SALESMAN REFERRAL QR — CORRECT DOMAIN + SCAN-TO-BUY + COMMISSION (`aceab325`).** 5 gaps fixed: (1) QR host hardcoded to `diemthuong.khachvip.online`; (2) `/r/{code}` was never a route (no nginx rule) + `|` not URL-safe in a path; (3) `Order` had Sprint-0 referral fields with **no domain setter**; (4) `CheckoutOrderRequest`/`Checkout.razor` never sent `ReferralCode`; (5) `CreateCommissionAsync` **never called outside tests**. Fix: QR → `{khachLinkOrigin}/scan?ref={escaped}` (origin from `SalesmanQR.razor` via `?sourceDomain=`), anonymous `GET /api/community/referral/{code}` → product info, `Scan.razor` resolves + adds to cart + stores code, checkout sends `ReferralCode`, Gateway → `Order.SetSalesmanReferral` (approved Domain addition), `OrderWorkflowService.HandleOrderCompletedAsync` creates the commission (optional Gateway-only `ISalesmanService`). QR canvas fluid on mobile. Build 0 errors · Guard ALL PASSED · 21/21 tests · CD Multi-VPS SUCCESS. RV: QR URL `https://diemthuong2.khachvip.online/scan?ref=DL9ZMQ%7CCOMRV1`; anonymous resolve 200; checkout set `SalesmanId`/`ReferralProductId`/`ReferralCode`; shipper delivered → completed → `SalesReferrals` 1650.00 (55000 × 0.03, Pending); salesman commissions API 1650. Task card: `task_card_06_referral_qr_scan_commission.md`. Temp `DevToken__Secret` re-used then removed.
